/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010BridgeTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010BridgeTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010BridgeTable is subid 1 of rhm010Bridges.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010BridgeTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010BridgeTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010BridgeTable_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTable_undo_setup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010BridgeTable undo.
     * set up rhm010BridgeTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010BridgeSync_val_ptr )*
     */

    return rc;
}                               /* rhm010BridgeTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010BridgeTable_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010BridgeTable undo.
     * rhm010BridgeTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010BridgeSync_val_ptr )*
     */

    return rc;
}                               /* rhm010BridgeTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BridgeTable_undo_cleanup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTable_undo_cleanup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010BridgeTable undo.
     * Undo storage is in (* rhm010BridgeSync_val_ptr )*
     */

    return rc;
}                               /* rhm010BridgeTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BridgeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010BridgeTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BridgeTable_commit(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTable_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010BridgeTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010BRIDGELINKREF_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGELINKREF_FLAG; /* clear rhm010BridgeLinkRef */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeLinkRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeLinkRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeLinkRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGELINKREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGECHANNELID_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGECHANNELID_FLAG;       /* clear rhm010BridgeChannelId */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeChannelId.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeChannelId commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeChannelId
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGECHANNELID_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGEDURABLE_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGEDURABLE_FLAG; /* clear rhm010BridgeDurable */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeDurable.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeDurable commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeDurable
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGEDURABLE_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGESRC_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGESRC_FLAG;     /* clear rhm010BridgeSrc */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeSrc.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeSrc commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeSrc
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010BRIDGESRC_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGEDEST_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGEDEST_FLAG;    /* clear rhm010BridgeDest */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeDest.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeDest commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeDest
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010BRIDGEDEST_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGEKEY_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGEKEY_FLAG;     /* clear rhm010BridgeKey */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeKey.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeKey commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeKey
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010BRIDGEKEY_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGESRCISQUEUE_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGESRCISQUEUE_FLAG;      /* clear rhm010BridgeSrcIsQueue */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeSrcIsQueue.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeSrcIsQueue commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeSrcIsQueue
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGESRCISQUEUE_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGESRCISLOCAL_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGESRCISLOCAL_FLAG;      /* clear rhm010BridgeSrcIsLocal */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeSrcIsLocal.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeSrcIsLocal commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeSrcIsLocal
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGESRCISLOCAL_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGETAG_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGETAG_FLAG;     /* clear rhm010BridgeTag */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeTag.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeTag commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeTag
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010BRIDGETAG_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGEEXCLUDES_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGEEXCLUDES_FLAG;        /* clear rhm010BridgeExcludes */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeExcludes.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeExcludes commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeExcludes
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGEEXCLUDES_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGEDYNAMIC_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGEDYNAMIC_FLAG; /* clear rhm010BridgeDynamic */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeDynamic.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeDynamic commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeDynamic
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BRIDGEDYNAMIC_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BRIDGESYNC_FLAG) {
        save_flags &= ~COLUMN_RHM010BRIDGESYNC_FLAG;    /* clear rhm010BridgeSync */
        /*
         * TODO:482:o: |-> commit column rhm010BridgeSync.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BridgeTable column rhm010BridgeSync commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BridgeSync
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010BRIDGESYNC_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010BridgeTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BridgeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010BridgeTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BridgeTable_undo_commit(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTable_undo_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010BridgeTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010BridgeTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010BridgeTable node value checks.
 * TODO:450:M: Implement rhm010BridgeTable undo functions.
 * TODO:460:M: Implement rhm010BridgeTable set functions.
 * TODO:480:M: Implement rhm010BridgeTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeLinkRef
 * rhm010BridgeLinkRef is subid 1 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.1
 * Description:
Bridge linkRef
                     Additional info ( nodeType:property, references:Link, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeLinkRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeLinkRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeLinkRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeLinkRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeLinkRef_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                                char *rhm010BridgeLinkRef_val_ptr,
                                size_t rhm010BridgeLinkRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeLinkRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeLinkRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeLinkRef value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeLinkRef value not illegal */
}                               /* rhm010BridgeLinkRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeLinkRef_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeLinkRef_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeLinkRef undo.
     */
    /*
     * copy rhm010BridgeLinkRef and rhm010BridgeLinkRef_len data
     * set rowreq_ctx->undo->rhm010BridgeLinkRef from rowreq_ctx->data.rhm010BridgeLinkRef
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeLinkRef,
           rowreq_ctx->data.rhm010BridgeLinkRef,
           (rowreq_ctx->data.rhm010BridgeLinkRef_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeLinkRef[0])));
    rowreq_ctx->undo->rhm010BridgeLinkRef_len =
        rowreq_ctx->data.rhm010BridgeLinkRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeLinkRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeLinkRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeLinkRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeLinkRef_val_ptr
 */
int
rhm010BridgeLinkRef_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                        char *rhm010BridgeLinkRef_val_ptr,
                        size_t rhm010BridgeLinkRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeLinkRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeLinkRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeLinkRef value.
     * set rhm010BridgeLinkRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeLinkRef,
           rhm010BridgeLinkRef_val_ptr, rhm010BridgeLinkRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeLinkRef_len =
        rhm010BridgeLinkRef_val_ptr_len /
        sizeof(rhm010BridgeLinkRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeLinkRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeLinkRef_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeLinkRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeLinkRef undo.
     */
    /*
     * copy rhm010BridgeLinkRef and rhm010BridgeLinkRef_len data
     * set rowreq_ctx->data.rhm010BridgeLinkRef from rowreq_ctx->undo->rhm010BridgeLinkRef
     */
    memcpy(rowreq_ctx->data.rhm010BridgeLinkRef,
           rowreq_ctx->undo->rhm010BridgeLinkRef,
           (rowreq_ctx->undo->rhm010BridgeLinkRef_len *
            sizeof(rowreq_ctx->data.rhm010BridgeLinkRef[0])));
    rowreq_ctx->data.rhm010BridgeLinkRef_len =
        rowreq_ctx->undo->rhm010BridgeLinkRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeLinkRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeChannelId
 * rhm010BridgeChannelId is subid 2 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.2
 * Description:
Bridge channelId
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   1
 *   hint: d
 *
 *
 * Its syntax is Uint16 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeChannelId_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeChannelId_check_value(rhm010BridgeTable_rowreq_ctx *
                                  rowreq_ctx,
                                  long rhm010BridgeChannelId_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeChannelId_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeChannelId value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeChannelId value not illegal */
}                               /* rhm010BridgeChannelId_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeChannelId_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeChannelId_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeChannelId undo.
     */
    /*
     * copy rhm010BridgeChannelId data
     * set rowreq_ctx->undo->rhm010BridgeChannelId from rowreq_ctx->data.rhm010BridgeChannelId
     */
    rowreq_ctx->undo->rhm010BridgeChannelId =
        rowreq_ctx->data.rhm010BridgeChannelId;


    return MFD_SUCCESS;
}                               /* rhm010BridgeChannelId_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeChannelId_val
 *        A long containing the new value.
 */
int
rhm010BridgeChannelId_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                          long rhm010BridgeChannelId_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeChannelId_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeChannelId value.
     * set rhm010BridgeChannelId value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeChannelId = rhm010BridgeChannelId_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeChannelId_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeChannelId_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeChannelId_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeChannelId undo.
     */
    /*
     * copy rhm010BridgeChannelId data
     * set rowreq_ctx->data.rhm010BridgeChannelId from rowreq_ctx->undo->rhm010BridgeChannelId
     */
    rowreq_ctx->data.rhm010BridgeChannelId =
        rowreq_ctx->undo->rhm010BridgeChannelId;


    return MFD_SUCCESS;
}                               /* rhm010BridgeChannelId_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeDurable
 * rhm010BridgeDurable is subid 3 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.3
 * Description:
Bridge durable
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeDurable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeDurable_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                                u_long rhm010BridgeDurable_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDurable_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeDurable value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeDurable value not illegal */
}                               /* rhm010BridgeDurable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeDurable_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDurable_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeDurable undo.
     */
    /*
     * copy rhm010BridgeDurable data
     * set rowreq_ctx->undo->rhm010BridgeDurable from rowreq_ctx->data.rhm010BridgeDurable
     */
    rowreq_ctx->undo->rhm010BridgeDurable =
        rowreq_ctx->data.rhm010BridgeDurable;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDurable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeDurable_val
 *        A long containing the new value.
 */
int
rhm010BridgeDurable_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                        u_long rhm010BridgeDurable_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDurable_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeDurable value.
     * set rhm010BridgeDurable value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeDurable = rhm010BridgeDurable_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeDurable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeDurable_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDurable_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeDurable undo.
     */
    /*
     * copy rhm010BridgeDurable data
     * set rowreq_ctx->data.rhm010BridgeDurable from rowreq_ctx->undo->rhm010BridgeDurable
     */
    rowreq_ctx->data.rhm010BridgeDurable =
        rowreq_ctx->undo->rhm010BridgeDurable;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDurable_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeSrc
 * rhm010BridgeSrc is subid 4 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.4
 * Description:
Bridge src
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeSrc_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeSrc_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeSrc_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeSrc).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeSrc_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                            char *rhm010BridgeSrc_val_ptr,
                            size_t rhm010BridgeSrc_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrc_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeSrc_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeSrc value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeSrc value not illegal */
}                               /* rhm010BridgeSrc_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeSrc_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrc_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeSrc undo.
     */
    /*
     * copy rhm010BridgeSrc and rhm010BridgeSrc_len data
     * set rowreq_ctx->undo->rhm010BridgeSrc from rowreq_ctx->data.rhm010BridgeSrc
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeSrc,
           rowreq_ctx->data.rhm010BridgeSrc,
           (rowreq_ctx->data.rhm010BridgeSrc_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeSrc[0])));
    rowreq_ctx->undo->rhm010BridgeSrc_len =
        rowreq_ctx->data.rhm010BridgeSrc_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrc_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeSrc_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeSrc_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeSrc_val_ptr
 */
int
rhm010BridgeSrc_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                    char *rhm010BridgeSrc_val_ptr,
                    size_t rhm010BridgeSrc_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrc_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeSrc_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeSrc value.
     * set rhm010BridgeSrc value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeSrc, rhm010BridgeSrc_val_ptr,
           rhm010BridgeSrc_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeSrc_len =
        rhm010BridgeSrc_val_ptr_len / sizeof(rhm010BridgeSrc_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeSrc_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeSrc_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrc_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeSrc undo.
     */
    /*
     * copy rhm010BridgeSrc and rhm010BridgeSrc_len data
     * set rowreq_ctx->data.rhm010BridgeSrc from rowreq_ctx->undo->rhm010BridgeSrc
     */
    memcpy(rowreq_ctx->data.rhm010BridgeSrc,
           rowreq_ctx->undo->rhm010BridgeSrc,
           (rowreq_ctx->undo->rhm010BridgeSrc_len *
            sizeof(rowreq_ctx->data.rhm010BridgeSrc[0])));
    rowreq_ctx->data.rhm010BridgeSrc_len =
        rowreq_ctx->undo->rhm010BridgeSrc_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrc_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeDest
 * rhm010BridgeDest is subid 5 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.5
 * Description:
Bridge dest
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeDest_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeDest_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeDest_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeDest).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeDest_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                             char *rhm010BridgeDest_val_ptr,
                             size_t rhm010BridgeDest_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDest_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeDest_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeDest value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeDest value not illegal */
}                               /* rhm010BridgeDest_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeDest_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDest_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeDest undo.
     */
    /*
     * copy rhm010BridgeDest and rhm010BridgeDest_len data
     * set rowreq_ctx->undo->rhm010BridgeDest from rowreq_ctx->data.rhm010BridgeDest
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeDest,
           rowreq_ctx->data.rhm010BridgeDest,
           (rowreq_ctx->data.rhm010BridgeDest_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeDest[0])));
    rowreq_ctx->undo->rhm010BridgeDest_len =
        rowreq_ctx->data.rhm010BridgeDest_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDest_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeDest_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeDest_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeDest_val_ptr
 */
int
rhm010BridgeDest_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                     char *rhm010BridgeDest_val_ptr,
                     size_t rhm010BridgeDest_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDest_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeDest_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeDest value.
     * set rhm010BridgeDest value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeDest, rhm010BridgeDest_val_ptr,
           rhm010BridgeDest_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeDest_len =
        rhm010BridgeDest_val_ptr_len / sizeof(rhm010BridgeDest_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeDest_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeDest_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDest_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeDest undo.
     */
    /*
     * copy rhm010BridgeDest and rhm010BridgeDest_len data
     * set rowreq_ctx->data.rhm010BridgeDest from rowreq_ctx->undo->rhm010BridgeDest
     */
    memcpy(rowreq_ctx->data.rhm010BridgeDest,
           rowreq_ctx->undo->rhm010BridgeDest,
           (rowreq_ctx->undo->rhm010BridgeDest_len *
            sizeof(rowreq_ctx->data.rhm010BridgeDest[0])));
    rowreq_ctx->data.rhm010BridgeDest_len =
        rowreq_ctx->undo->rhm010BridgeDest_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDest_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeKey
 * rhm010BridgeKey is subid 6 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.6
 * Description:
Bridge key
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Lstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeKey_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeKey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeKey).
 *    The length is in (one of) the range set(s):  0 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeKey_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                            char *rhm010BridgeKey_val_ptr,
                            size_t rhm010BridgeKey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeKey_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeKey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeKey value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeKey value not illegal */
}                               /* rhm010BridgeKey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeKey_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeKey_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeKey undo.
     */
    /*
     * copy rhm010BridgeKey and rhm010BridgeKey_len data
     * set rowreq_ctx->undo->rhm010BridgeKey from rowreq_ctx->data.rhm010BridgeKey
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeKey,
           rowreq_ctx->data.rhm010BridgeKey,
           (rowreq_ctx->data.rhm010BridgeKey_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeKey[0])));
    rowreq_ctx->undo->rhm010BridgeKey_len =
        rowreq_ctx->data.rhm010BridgeKey_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeKey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeKey_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeKey_val_ptr
 */
int
rhm010BridgeKey_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                    char *rhm010BridgeKey_val_ptr,
                    size_t rhm010BridgeKey_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeKey_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeKey_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeKey value.
     * set rhm010BridgeKey value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeKey, rhm010BridgeKey_val_ptr,
           rhm010BridgeKey_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeKey_len =
        rhm010BridgeKey_val_ptr_len / sizeof(rhm010BridgeKey_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeKey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeKey_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeKey_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeKey undo.
     */
    /*
     * copy rhm010BridgeKey and rhm010BridgeKey_len data
     * set rowreq_ctx->data.rhm010BridgeKey from rowreq_ctx->undo->rhm010BridgeKey
     */
    memcpy(rowreq_ctx->data.rhm010BridgeKey,
           rowreq_ctx->undo->rhm010BridgeKey,
           (rowreq_ctx->undo->rhm010BridgeKey_len *
            sizeof(rowreq_ctx->data.rhm010BridgeKey[0])));
    rowreq_ctx->data.rhm010BridgeKey_len =
        rowreq_ctx->undo->rhm010BridgeKey_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeKey_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeSrcIsQueue
 * rhm010BridgeSrcIsQueue is subid 7 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.7
 * Description:
Bridge srcIsQueue
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeSrcIsQueue_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeSrcIsQueue_check_value(rhm010BridgeTable_rowreq_ctx *
                                   rowreq_ctx,
                                   u_long rhm010BridgeSrcIsQueue_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsQueue_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeSrcIsQueue value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeSrcIsQueue value not illegal */
}                               /* rhm010BridgeSrcIsQueue_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeSrcIsQueue_undo_setup(rhm010BridgeTable_rowreq_ctx *
                                  rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsQueue_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeSrcIsQueue undo.
     */
    /*
     * copy rhm010BridgeSrcIsQueue data
     * set rowreq_ctx->undo->rhm010BridgeSrcIsQueue from rowreq_ctx->data.rhm010BridgeSrcIsQueue
     */
    rowreq_ctx->undo->rhm010BridgeSrcIsQueue =
        rowreq_ctx->data.rhm010BridgeSrcIsQueue;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsQueue_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeSrcIsQueue_val
 *        A long containing the new value.
 */
int
rhm010BridgeSrcIsQueue_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                           u_long rhm010BridgeSrcIsQueue_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsQueue_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeSrcIsQueue value.
     * set rhm010BridgeSrcIsQueue value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeSrcIsQueue = rhm010BridgeSrcIsQueue_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsQueue_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeSrcIsQueue_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsQueue_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeSrcIsQueue undo.
     */
    /*
     * copy rhm010BridgeSrcIsQueue data
     * set rowreq_ctx->data.rhm010BridgeSrcIsQueue from rowreq_ctx->undo->rhm010BridgeSrcIsQueue
     */
    rowreq_ctx->data.rhm010BridgeSrcIsQueue =
        rowreq_ctx->undo->rhm010BridgeSrcIsQueue;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsQueue_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeSrcIsLocal
 * rhm010BridgeSrcIsLocal is subid 8 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.8
 * Description:
Bridge srcIsLocal
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeSrcIsLocal_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeSrcIsLocal_check_value(rhm010BridgeTable_rowreq_ctx *
                                   rowreq_ctx,
                                   u_long rhm010BridgeSrcIsLocal_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsLocal_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeSrcIsLocal value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeSrcIsLocal value not illegal */
}                               /* rhm010BridgeSrcIsLocal_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeSrcIsLocal_undo_setup(rhm010BridgeTable_rowreq_ctx *
                                  rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsLocal_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeSrcIsLocal undo.
     */
    /*
     * copy rhm010BridgeSrcIsLocal data
     * set rowreq_ctx->undo->rhm010BridgeSrcIsLocal from rowreq_ctx->data.rhm010BridgeSrcIsLocal
     */
    rowreq_ctx->undo->rhm010BridgeSrcIsLocal =
        rowreq_ctx->data.rhm010BridgeSrcIsLocal;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsLocal_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeSrcIsLocal_val
 *        A long containing the new value.
 */
int
rhm010BridgeSrcIsLocal_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                           u_long rhm010BridgeSrcIsLocal_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsLocal_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeSrcIsLocal value.
     * set rhm010BridgeSrcIsLocal value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeSrcIsLocal = rhm010BridgeSrcIsLocal_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsLocal_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeSrcIsLocal_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSrcIsLocal_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeSrcIsLocal undo.
     */
    /*
     * copy rhm010BridgeSrcIsLocal data
     * set rowreq_ctx->data.rhm010BridgeSrcIsLocal from rowreq_ctx->undo->rhm010BridgeSrcIsLocal
     */
    rowreq_ctx->data.rhm010BridgeSrcIsLocal =
        rowreq_ctx->undo->rhm010BridgeSrcIsLocal;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSrcIsLocal_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeTag
 * rhm010BridgeTag is subid 9 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.9
 * Description:
Bridge tag
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeTag_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeTag_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeTag_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeTag).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeTag_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                            char *rhm010BridgeTag_val_ptr,
                            size_t rhm010BridgeTag_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTag_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeTag_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeTag value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeTag value not illegal */
}                               /* rhm010BridgeTag_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeTag_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTag_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeTag undo.
     */
    /*
     * copy rhm010BridgeTag and rhm010BridgeTag_len data
     * set rowreq_ctx->undo->rhm010BridgeTag from rowreq_ctx->data.rhm010BridgeTag
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeTag,
           rowreq_ctx->data.rhm010BridgeTag,
           (rowreq_ctx->data.rhm010BridgeTag_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeTag[0])));
    rowreq_ctx->undo->rhm010BridgeTag_len =
        rowreq_ctx->data.rhm010BridgeTag_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeTag_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeTag_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeTag_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeTag_val_ptr
 */
int
rhm010BridgeTag_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                    char *rhm010BridgeTag_val_ptr,
                    size_t rhm010BridgeTag_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTag_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeTag_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeTag value.
     * set rhm010BridgeTag value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeTag, rhm010BridgeTag_val_ptr,
           rhm010BridgeTag_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeTag_len =
        rhm010BridgeTag_val_ptr_len / sizeof(rhm010BridgeTag_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeTag_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeTag_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeTag_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeTag undo.
     */
    /*
     * copy rhm010BridgeTag and rhm010BridgeTag_len data
     * set rowreq_ctx->data.rhm010BridgeTag from rowreq_ctx->undo->rhm010BridgeTag
     */
    memcpy(rowreq_ctx->data.rhm010BridgeTag,
           rowreq_ctx->undo->rhm010BridgeTag,
           (rowreq_ctx->undo->rhm010BridgeTag_len *
            sizeof(rowreq_ctx->data.rhm010BridgeTag[0])));
    rowreq_ctx->data.rhm010BridgeTag_len =
        rowreq_ctx->undo->rhm010BridgeTag_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeTag_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeExcludes
 * rhm010BridgeExcludes is subid 10 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.10
 * Description:
Bridge excludes
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeExcludes_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeExcludes_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeExcludes_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BridgeExcludes).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeExcludes_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                                 char *rhm010BridgeExcludes_val_ptr,
                                 size_t rhm010BridgeExcludes_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeExcludes_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeExcludes_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeExcludes value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeExcludes value not illegal */
}                               /* rhm010BridgeExcludes_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeExcludes_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeExcludes_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeExcludes undo.
     */
    /*
     * copy rhm010BridgeExcludes and rhm010BridgeExcludes_len data
     * set rowreq_ctx->undo->rhm010BridgeExcludes from rowreq_ctx->data.rhm010BridgeExcludes
     */
    memcpy(rowreq_ctx->undo->rhm010BridgeExcludes,
           rowreq_ctx->data.rhm010BridgeExcludes,
           (rowreq_ctx->data.rhm010BridgeExcludes_len *
            sizeof(rowreq_ctx->undo->rhm010BridgeExcludes[0])));
    rowreq_ctx->undo->rhm010BridgeExcludes_len =
        rowreq_ctx->data.rhm010BridgeExcludes_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeExcludes_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeExcludes_val_ptr
 *        A char containing the new value.
 * @param rhm010BridgeExcludes_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BridgeExcludes_val_ptr
 */
int
rhm010BridgeExcludes_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                         char *rhm010BridgeExcludes_val_ptr,
                         size_t rhm010BridgeExcludes_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeExcludes_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BridgeExcludes_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BridgeExcludes value.
     * set rhm010BridgeExcludes value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BridgeExcludes,
           rhm010BridgeExcludes_val_ptr, rhm010BridgeExcludes_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BridgeExcludes_len =
        rhm010BridgeExcludes_val_ptr_len /
        sizeof(rhm010BridgeExcludes_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BridgeExcludes_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeExcludes_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeExcludes_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeExcludes undo.
     */
    /*
     * copy rhm010BridgeExcludes and rhm010BridgeExcludes_len data
     * set rowreq_ctx->data.rhm010BridgeExcludes from rowreq_ctx->undo->rhm010BridgeExcludes
     */
    memcpy(rowreq_ctx->data.rhm010BridgeExcludes,
           rowreq_ctx->undo->rhm010BridgeExcludes,
           (rowreq_ctx->undo->rhm010BridgeExcludes_len *
            sizeof(rowreq_ctx->data.rhm010BridgeExcludes[0])));
    rowreq_ctx->data.rhm010BridgeExcludes_len =
        rowreq_ctx->undo->rhm010BridgeExcludes_len;


    return MFD_SUCCESS;
}                               /* rhm010BridgeExcludes_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeDynamic
 * rhm010BridgeDynamic is subid 11 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.11
 * Description:
Bridge dynamic
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeDynamic_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeDynamic_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                                u_long rhm010BridgeDynamic_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDynamic_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeDynamic value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeDynamic value not illegal */
}                               /* rhm010BridgeDynamic_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeDynamic_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDynamic_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeDynamic undo.
     */
    /*
     * copy rhm010BridgeDynamic data
     * set rowreq_ctx->undo->rhm010BridgeDynamic from rowreq_ctx->data.rhm010BridgeDynamic
     */
    rowreq_ctx->undo->rhm010BridgeDynamic =
        rowreq_ctx->data.rhm010BridgeDynamic;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDynamic_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeDynamic_val
 *        A long containing the new value.
 */
int
rhm010BridgeDynamic_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                        u_long rhm010BridgeDynamic_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDynamic_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeDynamic value.
     * set rhm010BridgeDynamic value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeDynamic = rhm010BridgeDynamic_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeDynamic_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeDynamic_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeDynamic_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeDynamic undo.
     */
    /*
     * copy rhm010BridgeDynamic data
     * set rowreq_ctx->data.rhm010BridgeDynamic from rowreq_ctx->undo->rhm010BridgeDynamic
     */
    rowreq_ctx->data.rhm010BridgeDynamic =
        rowreq_ctx->undo->rhm010BridgeDynamic;


    return MFD_SUCCESS;
}                               /* rhm010BridgeDynamic_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BridgeEntry.rhm010BridgeSync
 * rhm010BridgeSync is subid 12 of rhm010BridgeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.12.1.1.12
 * Description:
Bridge sync
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   1
 *   hint: d
 *
 *
 * Its syntax is Uint16 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BridgeSync_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BridgeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BridgeSync_check_value(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                             long rhm010BridgeSync_val)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSync_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010BridgeSync value.
     */

    return MFD_SUCCESS;         /* rhm010BridgeSync value not illegal */
}                               /* rhm010BridgeSync_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BridgeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BridgeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BridgeSync_undo_setup(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSync_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BridgeSync undo.
     */
    /*
     * copy rhm010BridgeSync data
     * set rowreq_ctx->undo->rhm010BridgeSync from rowreq_ctx->data.rhm010BridgeSync
     */
    rowreq_ctx->undo->rhm010BridgeSync = rowreq_ctx->data.rhm010BridgeSync;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSync_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BridgeSync_val
 *        A long containing the new value.
 */
int
rhm010BridgeSync_set(rhm010BridgeTable_rowreq_ctx * rowreq_ctx,
                     long rhm010BridgeSync_val)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSync_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010BridgeSync value.
     * set rhm010BridgeSync value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010BridgeSync = rhm010BridgeSync_val;

    return MFD_SUCCESS;
}                               /* rhm010BridgeSync_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BridgeSync_undo(rhm010BridgeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BridgeTable:rhm010BridgeSync_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BridgeSync undo.
     */
    /*
     * copy rhm010BridgeSync data
     * set rowreq_ctx->data.rhm010BridgeSync from rowreq_ctx->undo->rhm010BridgeSync
     */
    rowreq_ctx->data.rhm010BridgeSync = rowreq_ctx->undo->rhm010BridgeSync;


    return MFD_SUCCESS;
}                               /* rhm010BridgeSync_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BridgeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010BridgeTable if you don't have dependencies)
 */
int
rhm010BridgeTable_check_dependencies(rhm010BridgeTable_rowreq_ctx *
                                     rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010BridgeTable:rhm010BridgeTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010BridgeTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010BridgeTable_check_dependencies */

/** @} */
