/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010SubscriptionTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010SubscriptionTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010SubscriptionTable is subid 1 of rhm010Subscriptions.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010SubscriptionTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010SubscriptionTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010SubscriptionTable_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                   rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionTable_undo_setup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010SubscriptionTable undo.
     * set up rhm010SubscriptionTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010SubscriptionDelivered_val_ptr )*
     */

    return rc;
}                               /* rhm010SubscriptionTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010SubscriptionTable_undo(rhm010SubscriptionTable_rowreq_ctx *
                             rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionTable_undo", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010SubscriptionTable undo.
     * rhm010SubscriptionTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010SubscriptionDelivered_val_ptr )*
     */

    return rc;
}                               /* rhm010SubscriptionTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SubscriptionTable_undo_cleanup(rhm010SubscriptionTable_rowreq_ctx *
                                     rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010SubscriptionTable undo.
     * Undo storage is in (* rhm010SubscriptionDelivered_val_ptr )*
     */

    return rc;
}                               /* rhm010SubscriptionTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SubscriptionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010SubscriptionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SubscriptionTable_commit(rhm010SubscriptionTable_rowreq_ctx *
                               rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionTable_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010SubscriptionTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010SUBSCRIPTIONSESSIONREF_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONSESSIONREF_FLAG;        /* clear rhm010SubscriptionSessionRef */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionSessionRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionSessionRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionSessionRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONSESSIONREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONQUEUEREF_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONQUEUEREF_FLAG;  /* clear rhm010SubscriptionQueueRef */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionQueueRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionQueueRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionQueueRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONQUEUEREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONNAME_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONNAME_FLAG;      /* clear rhm010SubscriptionName */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionName.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionName commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionName
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONNAME_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONBROWSING_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONBROWSING_FLAG;  /* clear rhm010SubscriptionBrowsing */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionBrowsing.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionBrowsing commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionBrowsing
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONBROWSING_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONACKNOWLEDGED_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONACKNOWLEDGED_FLAG;      /* clear rhm010SubscriptionAcknowledged */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionAcknowledged.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionAcknowledged commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionAcknowledged
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONACKNOWLEDGED_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONEXCLUSIVE_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONEXCLUSIVE_FLAG; /* clear rhm010SubscriptionExclusive */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionExclusive.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionExclusive commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionExclusive
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONEXCLUSIVE_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SUBSCRIPTIONARGUMENTS_FLAG) {
        save_flags &= ~COLUMN_RHM010SUBSCRIPTIONARGUMENTS_FLAG; /* clear rhm010SubscriptionArguments */
        /*
         * TODO:482:o: |-> commit column rhm010SubscriptionArguments.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SubscriptionTable column rhm010SubscriptionArguments commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SubscriptionArguments
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SUBSCRIPTIONARGUMENTS_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010SubscriptionTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SubscriptionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010SubscriptionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SubscriptionTable_undo_commit(rhm010SubscriptionTable_rowreq_ctx *
                                    rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010SubscriptionTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010SubscriptionTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010SubscriptionTable node value checks.
 * TODO:450:M: Implement rhm010SubscriptionTable undo functions.
 * TODO:460:M: Implement rhm010SubscriptionTable set functions.
 * TODO:480:M: Implement rhm010SubscriptionTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionSessionRef
 * rhm010SubscriptionSessionRef is subid 1 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.1
 * Description:
Subscription sessionRef
                     Additional info ( nodeType:property, references:Session, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionSessionRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionSessionRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionSessionRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SubscriptionSessionRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionSessionRef_check_value(rhm010SubscriptionTable_rowreq_ctx
                                         * rowreq_ctx,
                                         char
                                         *rhm010SubscriptionSessionRef_val_ptr,
                                         size_t
                                         rhm010SubscriptionSessionRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionSessionRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionSessionRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionSessionRef value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionSessionRef value not illegal */
}                               /* rhm010SubscriptionSessionRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionSessionRef_undo_setup(rhm010SubscriptionTable_rowreq_ctx
                                        * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionSessionRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionSessionRef undo.
     */
    /*
     * copy rhm010SubscriptionSessionRef and rhm010SubscriptionSessionRef_len data
     * set rowreq_ctx->undo->rhm010SubscriptionSessionRef from rowreq_ctx->data.rhm010SubscriptionSessionRef
     */
    memcpy(rowreq_ctx->undo->rhm010SubscriptionSessionRef,
           rowreq_ctx->data.rhm010SubscriptionSessionRef,
           (rowreq_ctx->data.rhm010SubscriptionSessionRef_len *
            sizeof(rowreq_ctx->undo->rhm010SubscriptionSessionRef[0])));
    rowreq_ctx->undo->rhm010SubscriptionSessionRef_len =
        rowreq_ctx->data.rhm010SubscriptionSessionRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionSessionRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionSessionRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionSessionRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionSessionRef_val_ptr
 */
int
rhm010SubscriptionSessionRef_set(rhm010SubscriptionTable_rowreq_ctx *
                                 rowreq_ctx,
                                 char
                                 *rhm010SubscriptionSessionRef_val_ptr,
                                 size_t
                                 rhm010SubscriptionSessionRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionSessionRef_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionSessionRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionSessionRef value.
     * set rhm010SubscriptionSessionRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionSessionRef,
           rhm010SubscriptionSessionRef_val_ptr,
           rhm010SubscriptionSessionRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SubscriptionSessionRef_len =
        rhm010SubscriptionSessionRef_val_ptr_len /
        sizeof(rhm010SubscriptionSessionRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionSessionRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionSessionRef_undo(rhm010SubscriptionTable_rowreq_ctx *
                                  rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionSessionRef_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionSessionRef undo.
     */
    /*
     * copy rhm010SubscriptionSessionRef and rhm010SubscriptionSessionRef_len data
     * set rowreq_ctx->data.rhm010SubscriptionSessionRef from rowreq_ctx->undo->rhm010SubscriptionSessionRef
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionSessionRef,
           rowreq_ctx->undo->rhm010SubscriptionSessionRef,
           (rowreq_ctx->undo->rhm010SubscriptionSessionRef_len *
            sizeof(rowreq_ctx->data.rhm010SubscriptionSessionRef[0])));
    rowreq_ctx->data.rhm010SubscriptionSessionRef_len =
        rowreq_ctx->undo->rhm010SubscriptionSessionRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionSessionRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionQueueRef
 * rhm010SubscriptionQueueRef is subid 2 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.2
 * Description:
Subscription queueRef
                     Additional info ( nodeType:property, references:Queue, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionQueueRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionQueueRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionQueueRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SubscriptionQueueRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionQueueRef_check_value(rhm010SubscriptionTable_rowreq_ctx *
                                       rowreq_ctx,
                                       char
                                       *rhm010SubscriptionQueueRef_val_ptr,
                                       size_t
                                       rhm010SubscriptionQueueRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionQueueRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionQueueRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionQueueRef value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionQueueRef value not illegal */
}                               /* rhm010SubscriptionQueueRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionQueueRef_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                      rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionQueueRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionQueueRef undo.
     */
    /*
     * copy rhm010SubscriptionQueueRef and rhm010SubscriptionQueueRef_len data
     * set rowreq_ctx->undo->rhm010SubscriptionQueueRef from rowreq_ctx->data.rhm010SubscriptionQueueRef
     */
    memcpy(rowreq_ctx->undo->rhm010SubscriptionQueueRef,
           rowreq_ctx->data.rhm010SubscriptionQueueRef,
           (rowreq_ctx->data.rhm010SubscriptionQueueRef_len *
            sizeof(rowreq_ctx->undo->rhm010SubscriptionQueueRef[0])));
    rowreq_ctx->undo->rhm010SubscriptionQueueRef_len =
        rowreq_ctx->data.rhm010SubscriptionQueueRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionQueueRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionQueueRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionQueueRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionQueueRef_val_ptr
 */
int
rhm010SubscriptionQueueRef_set(rhm010SubscriptionTable_rowreq_ctx *
                               rowreq_ctx,
                               char *rhm010SubscriptionQueueRef_val_ptr,
                               size_t
                               rhm010SubscriptionQueueRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionQueueRef_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionQueueRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionQueueRef value.
     * set rhm010SubscriptionQueueRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionQueueRef,
           rhm010SubscriptionQueueRef_val_ptr,
           rhm010SubscriptionQueueRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SubscriptionQueueRef_len =
        rhm010SubscriptionQueueRef_val_ptr_len /
        sizeof(rhm010SubscriptionQueueRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionQueueRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionQueueRef_undo(rhm010SubscriptionTable_rowreq_ctx *
                                rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionQueueRef_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionQueueRef undo.
     */
    /*
     * copy rhm010SubscriptionQueueRef and rhm010SubscriptionQueueRef_len data
     * set rowreq_ctx->data.rhm010SubscriptionQueueRef from rowreq_ctx->undo->rhm010SubscriptionQueueRef
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionQueueRef,
           rowreq_ctx->undo->rhm010SubscriptionQueueRef,
           (rowreq_ctx->undo->rhm010SubscriptionQueueRef_len *
            sizeof(rowreq_ctx->data.rhm010SubscriptionQueueRef[0])));
    rowreq_ctx->data.rhm010SubscriptionQueueRef_len =
        rowreq_ctx->undo->rhm010SubscriptionQueueRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionQueueRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionName
 * rhm010SubscriptionName is subid 3 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.3
 * Description:
Subscription name
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionName_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SubscriptionName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionName_check_value(rhm010SubscriptionTable_rowreq_ctx *
                                   rowreq_ctx,
                                   char *rhm010SubscriptionName_val_ptr,
                                   size_t
                                   rhm010SubscriptionName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionName_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionName value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionName value not illegal */
}                               /* rhm010SubscriptionName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionName_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                  rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionName_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionName undo.
     */
    /*
     * copy rhm010SubscriptionName and rhm010SubscriptionName_len data
     * set rowreq_ctx->undo->rhm010SubscriptionName from rowreq_ctx->data.rhm010SubscriptionName
     */
    memcpy(rowreq_ctx->undo->rhm010SubscriptionName,
           rowreq_ctx->data.rhm010SubscriptionName,
           (rowreq_ctx->data.rhm010SubscriptionName_len *
            sizeof(rowreq_ctx->undo->rhm010SubscriptionName[0])));
    rowreq_ctx->undo->rhm010SubscriptionName_len =
        rowreq_ctx->data.rhm010SubscriptionName_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionName_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionName_val_ptr
 */
int
rhm010SubscriptionName_set(rhm010SubscriptionTable_rowreq_ctx * rowreq_ctx,
                           char *rhm010SubscriptionName_val_ptr,
                           size_t rhm010SubscriptionName_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionName_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionName_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionName value.
     * set rhm010SubscriptionName value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionName,
           rhm010SubscriptionName_val_ptr,
           rhm010SubscriptionName_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SubscriptionName_len =
        rhm010SubscriptionName_val_ptr_len /
        sizeof(rhm010SubscriptionName_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionName_undo(rhm010SubscriptionTable_rowreq_ctx *
                            rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionName_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionName undo.
     */
    /*
     * copy rhm010SubscriptionName and rhm010SubscriptionName_len data
     * set rowreq_ctx->data.rhm010SubscriptionName from rowreq_ctx->undo->rhm010SubscriptionName
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionName,
           rowreq_ctx->undo->rhm010SubscriptionName,
           (rowreq_ctx->undo->rhm010SubscriptionName_len *
            sizeof(rowreq_ctx->data.rhm010SubscriptionName[0])));
    rowreq_ctx->data.rhm010SubscriptionName_len =
        rowreq_ctx->undo->rhm010SubscriptionName_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionName_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionBrowsing
 * rhm010SubscriptionBrowsing is subid 4 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.4
 * Description:
Subscription browsing
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionBrowsing_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionBrowsing_check_value(rhm010SubscriptionTable_rowreq_ctx *
                                       rowreq_ctx,
                                       u_long
                                       rhm010SubscriptionBrowsing_val)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionBrowsing_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionBrowsing value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionBrowsing value not illegal */
}                               /* rhm010SubscriptionBrowsing_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionBrowsing_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                      rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionBrowsing_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionBrowsing undo.
     */
    /*
     * copy rhm010SubscriptionBrowsing data
     * set rowreq_ctx->undo->rhm010SubscriptionBrowsing from rowreq_ctx->data.rhm010SubscriptionBrowsing
     */
    rowreq_ctx->undo->rhm010SubscriptionBrowsing =
        rowreq_ctx->data.rhm010SubscriptionBrowsing;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionBrowsing_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionBrowsing_val
 *        A long containing the new value.
 */
int
rhm010SubscriptionBrowsing_set(rhm010SubscriptionTable_rowreq_ctx *
                               rowreq_ctx,
                               u_long rhm010SubscriptionBrowsing_val)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionBrowsing_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionBrowsing value.
     * set rhm010SubscriptionBrowsing value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010SubscriptionBrowsing =
        rhm010SubscriptionBrowsing_val;

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionBrowsing_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionBrowsing_undo(rhm010SubscriptionTable_rowreq_ctx *
                                rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionBrowsing_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionBrowsing undo.
     */
    /*
     * copy rhm010SubscriptionBrowsing data
     * set rowreq_ctx->data.rhm010SubscriptionBrowsing from rowreq_ctx->undo->rhm010SubscriptionBrowsing
     */
    rowreq_ctx->data.rhm010SubscriptionBrowsing =
        rowreq_ctx->undo->rhm010SubscriptionBrowsing;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionBrowsing_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionAcknowledged
 * rhm010SubscriptionAcknowledged is subid 5 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.5
 * Description:
Subscription acknowledged
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionAcknowledged_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionAcknowledged_check_value
    (rhm010SubscriptionTable_rowreq_ctx * rowreq_ctx,
     u_long rhm010SubscriptionAcknowledged_val)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionAcknowledged_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionAcknowledged value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionAcknowledged value not illegal */
}                               /* rhm010SubscriptionAcknowledged_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionAcknowledged_undo_setup
    (rhm010SubscriptionTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionAcknowledged_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionAcknowledged undo.
     */
    /*
     * copy rhm010SubscriptionAcknowledged data
     * set rowreq_ctx->undo->rhm010SubscriptionAcknowledged from rowreq_ctx->data.rhm010SubscriptionAcknowledged
     */
    rowreq_ctx->undo->rhm010SubscriptionAcknowledged =
        rowreq_ctx->data.rhm010SubscriptionAcknowledged;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionAcknowledged_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionAcknowledged_val
 *        A long containing the new value.
 */
int
rhm010SubscriptionAcknowledged_set(rhm010SubscriptionTable_rowreq_ctx *
                                   rowreq_ctx,
                                   u_long
                                   rhm010SubscriptionAcknowledged_val)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionAcknowledged_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionAcknowledged value.
     * set rhm010SubscriptionAcknowledged value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010SubscriptionAcknowledged =
        rhm010SubscriptionAcknowledged_val;

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionAcknowledged_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionAcknowledged_undo(rhm010SubscriptionTable_rowreq_ctx *
                                    rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionAcknowledged_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionAcknowledged undo.
     */
    /*
     * copy rhm010SubscriptionAcknowledged data
     * set rowreq_ctx->data.rhm010SubscriptionAcknowledged from rowreq_ctx->undo->rhm010SubscriptionAcknowledged
     */
    rowreq_ctx->data.rhm010SubscriptionAcknowledged =
        rowreq_ctx->undo->rhm010SubscriptionAcknowledged;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionAcknowledged_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionExclusive
 * rhm010SubscriptionExclusive is subid 6 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.6
 * Description:
Subscription exclusive
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionExclusive_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionExclusive_check_value(rhm010SubscriptionTable_rowreq_ctx
                                        * rowreq_ctx,
                                        u_long
                                        rhm010SubscriptionExclusive_val)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionExclusive_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionExclusive value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionExclusive value not illegal */
}                               /* rhm010SubscriptionExclusive_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionExclusive_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                       rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionExclusive_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionExclusive undo.
     */
    /*
     * copy rhm010SubscriptionExclusive data
     * set rowreq_ctx->undo->rhm010SubscriptionExclusive from rowreq_ctx->data.rhm010SubscriptionExclusive
     */
    rowreq_ctx->undo->rhm010SubscriptionExclusive =
        rowreq_ctx->data.rhm010SubscriptionExclusive;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionExclusive_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionExclusive_val
 *        A long containing the new value.
 */
int
rhm010SubscriptionExclusive_set(rhm010SubscriptionTable_rowreq_ctx *
                                rowreq_ctx,
                                u_long rhm010SubscriptionExclusive_val)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionExclusive_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionExclusive value.
     * set rhm010SubscriptionExclusive value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010SubscriptionExclusive =
        rhm010SubscriptionExclusive_val;

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionExclusive_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionExclusive_undo(rhm010SubscriptionTable_rowreq_ctx *
                                 rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionExclusive_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionExclusive undo.
     */
    /*
     * copy rhm010SubscriptionExclusive data
     * set rowreq_ctx->data.rhm010SubscriptionExclusive from rowreq_ctx->undo->rhm010SubscriptionExclusive
     */
    rowreq_ctx->data.rhm010SubscriptionExclusive =
        rowreq_ctx->undo->rhm010SubscriptionExclusive;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionExclusive_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SubscriptionEntry.rhm010SubscriptionArguments
 * rhm010SubscriptionArguments is subid 8 of rhm010SubscriptionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.9.1.1.8
 * Description:
Subscription arguments
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Map (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SubscriptionArguments_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionArguments_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionArguments_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SubscriptionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SubscriptionArguments).
 *    The length is in (one of) the range set(s):  0 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SubscriptionArguments_check_value(rhm010SubscriptionTable_rowreq_ctx
                                        * rowreq_ctx,
                                        char
                                        *rhm010SubscriptionArguments_val_ptr,
                                        size_t
                                        rhm010SubscriptionArguments_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionArguments_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionArguments_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SubscriptionArguments value.
     */

    return MFD_SUCCESS;         /* rhm010SubscriptionArguments value not illegal */
}                               /* rhm010SubscriptionArguments_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SubscriptionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SubscriptionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SubscriptionArguments_undo_setup(rhm010SubscriptionTable_rowreq_ctx *
                                       rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionArguments_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SubscriptionArguments undo.
     */
    /*
     * copy rhm010SubscriptionArguments and rhm010SubscriptionArguments_len data
     * set rowreq_ctx->undo->rhm010SubscriptionArguments from rowreq_ctx->data.rhm010SubscriptionArguments
     */
    memcpy(rowreq_ctx->undo->rhm010SubscriptionArguments,
           rowreq_ctx->data.rhm010SubscriptionArguments,
           (rowreq_ctx->data.rhm010SubscriptionArguments_len *
            sizeof(rowreq_ctx->undo->rhm010SubscriptionArguments[0])));
    rowreq_ctx->undo->rhm010SubscriptionArguments_len =
        rowreq_ctx->data.rhm010SubscriptionArguments_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionArguments_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SubscriptionArguments_val_ptr
 *        A char containing the new value.
 * @param rhm010SubscriptionArguments_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SubscriptionArguments_val_ptr
 */
int
rhm010SubscriptionArguments_set(rhm010SubscriptionTable_rowreq_ctx *
                                rowreq_ctx,
                                char *rhm010SubscriptionArguments_val_ptr,
                                size_t
                                rhm010SubscriptionArguments_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionArguments_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SubscriptionArguments_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SubscriptionArguments value.
     * set rhm010SubscriptionArguments value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionArguments,
           rhm010SubscriptionArguments_val_ptr,
           rhm010SubscriptionArguments_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SubscriptionArguments_len =
        rhm010SubscriptionArguments_val_ptr_len /
        sizeof(rhm010SubscriptionArguments_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SubscriptionArguments_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SubscriptionArguments_undo(rhm010SubscriptionTable_rowreq_ctx *
                                 rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SubscriptionTable:rhm010SubscriptionArguments_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SubscriptionArguments undo.
     */
    /*
     * copy rhm010SubscriptionArguments and rhm010SubscriptionArguments_len data
     * set rowreq_ctx->data.rhm010SubscriptionArguments from rowreq_ctx->undo->rhm010SubscriptionArguments
     */
    memcpy(rowreq_ctx->data.rhm010SubscriptionArguments,
           rowreq_ctx->undo->rhm010SubscriptionArguments,
           (rowreq_ctx->undo->rhm010SubscriptionArguments_len *
            sizeof(rowreq_ctx->data.rhm010SubscriptionArguments[0])));
    rowreq_ctx->data.rhm010SubscriptionArguments_len =
        rowreq_ctx->undo->rhm010SubscriptionArguments_len;


    return MFD_SUCCESS;
}                               /* rhm010SubscriptionArguments_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SubscriptionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010SubscriptionTable if you don't have dependencies)
 */
int
rhm010SubscriptionTable_check_dependencies
    (rhm010SubscriptionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010SubscriptionTable:rhm010SubscriptionTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010SubscriptionTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010SubscriptionTable_check_dependencies */

/** @} */
