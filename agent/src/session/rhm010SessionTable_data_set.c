/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010SessionTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010SessionTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010SessionTable is subid 1 of rhm010Sessions.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.13.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010SessionTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010SessionTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SessionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010SessionTable_undo_setup(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionTable_undo_setup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010SessionTable undo.
     * set up rhm010SessionTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010SessionClientCredit_val_ptr )*
     */

    return rc;
}                               /* rhm010SessionTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SessionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010SessionTable_undo(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010SessionTable undo.
     * rhm010SessionTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010SessionClientCredit_val_ptr )*
     */

    return rc;
}                               /* rhm010SessionTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SessionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SessionTable_undo_cleanup(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010SessionTable undo.
     * Undo storage is in (* rhm010SessionClientCredit_val_ptr )*
     */

    return rc;
}                               /* rhm010SessionTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SessionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010SessionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SessionTable_commit(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionTable_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010SessionTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010SESSIONVHOSTREF_FLAG) {
        save_flags &= ~COLUMN_RHM010SESSIONVHOSTREF_FLAG;       /* clear rhm010SessionVhostRef */
        /*
         * TODO:482:o: |-> commit column rhm010SessionVhostRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SessionTable column rhm010SessionVhostRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SessionVhostRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010SESSIONVHOSTREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010SESSIONNAME_FLAG) {
        save_flags &= ~COLUMN_RHM010SESSIONNAME_FLAG;   /* clear rhm010SessionName */
        /*
         * TODO:482:o: |-> commit column rhm010SessionName.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010SessionTable column rhm010SessionName commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010SessionName
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010SESSIONNAME_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010SessionTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SessionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010SessionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010SessionTable_undo_commit(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010SessionTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010SessionTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010SessionTable node value checks.
 * TODO:450:M: Implement rhm010SessionTable undo functions.
 * TODO:460:M: Implement rhm010SessionTable set functions.
 * TODO:480:M: Implement rhm010SessionTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SessionEntry.rhm010SessionVhostRef
 * rhm010SessionVhostRef is subid 1 of rhm010SessionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.13.1.1.1
 * Description:
Session vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SessionVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SessionVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SessionVhostRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SessionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SessionVhostRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SessionVhostRef_check_value(rhm010SessionTable_rowreq_ctx *
                                  rowreq_ctx,
                                  char *rhm010SessionVhostRef_val_ptr,
                                  size_t rhm010SessionVhostRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionVhostRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SessionVhostRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SessionVhostRef value.
     */

    return MFD_SUCCESS;         /* rhm010SessionVhostRef value not illegal */
}                               /* rhm010SessionVhostRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SessionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SessionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SessionVhostRef_undo_setup(rhm010SessionTable_rowreq_ctx *
                                 rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionVhostRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SessionVhostRef undo.
     */
    /*
     * copy rhm010SessionVhostRef and rhm010SessionVhostRef_len data
     * set rowreq_ctx->undo->rhm010SessionVhostRef from rowreq_ctx->data.rhm010SessionVhostRef
     */
    memcpy(rowreq_ctx->undo->rhm010SessionVhostRef,
           rowreq_ctx->data.rhm010SessionVhostRef,
           (rowreq_ctx->data.rhm010SessionVhostRef_len *
            sizeof(rowreq_ctx->undo->rhm010SessionVhostRef[0])));
    rowreq_ctx->undo->rhm010SessionVhostRef_len =
        rowreq_ctx->data.rhm010SessionVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SessionVhostRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SessionVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010SessionVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SessionVhostRef_val_ptr
 */
int
rhm010SessionVhostRef_set(rhm010SessionTable_rowreq_ctx * rowreq_ctx,
                          char *rhm010SessionVhostRef_val_ptr,
                          size_t rhm010SessionVhostRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionVhostRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SessionVhostRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SessionVhostRef value.
     * set rhm010SessionVhostRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SessionVhostRef,
           rhm010SessionVhostRef_val_ptr,
           rhm010SessionVhostRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SessionVhostRef_len =
        rhm010SessionVhostRef_val_ptr_len /
        sizeof(rhm010SessionVhostRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SessionVhostRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SessionVhostRef_undo(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionVhostRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SessionVhostRef undo.
     */
    /*
     * copy rhm010SessionVhostRef and rhm010SessionVhostRef_len data
     * set rowreq_ctx->data.rhm010SessionVhostRef from rowreq_ctx->undo->rhm010SessionVhostRef
     */
    memcpy(rowreq_ctx->data.rhm010SessionVhostRef,
           rowreq_ctx->undo->rhm010SessionVhostRef,
           (rowreq_ctx->undo->rhm010SessionVhostRef_len *
            sizeof(rowreq_ctx->data.rhm010SessionVhostRef[0])));
    rowreq_ctx->data.rhm010SessionVhostRef_len =
        rowreq_ctx->undo->rhm010SessionVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010SessionVhostRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010SessionEntry.rhm010SessionName
 * rhm010SessionName is subid 2 of rhm010SessionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.13.1.1.2
 * Description:
Session name
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010SessionName_val_ptr
 *        A char containing the new value.
 * @param rhm010SessionName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SessionName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010SessionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010SessionName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010SessionName_check_value(rhm010SessionTable_rowreq_ctx * rowreq_ctx,
                              char *rhm010SessionName_val_ptr,
                              size_t rhm010SessionName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionName_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SessionName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010SessionName value.
     */

    return MFD_SUCCESS;         /* rhm010SessionName value not illegal */
}                               /* rhm010SessionName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010SessionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010SessionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010SessionName_undo_setup(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionName_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010SessionName undo.
     */
    /*
     * copy rhm010SessionName and rhm010SessionName_len data
     * set rowreq_ctx->undo->rhm010SessionName from rowreq_ctx->data.rhm010SessionName
     */
    memcpy(rowreq_ctx->undo->rhm010SessionName,
           rowreq_ctx->data.rhm010SessionName,
           (rowreq_ctx->data.rhm010SessionName_len *
            sizeof(rowreq_ctx->undo->rhm010SessionName[0])));
    rowreq_ctx->undo->rhm010SessionName_len =
        rowreq_ctx->data.rhm010SessionName_len;


    return MFD_SUCCESS;
}                               /* rhm010SessionName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010SessionName_val_ptr
 *        A char containing the new value.
 * @param rhm010SessionName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010SessionName_val_ptr
 */
int
rhm010SessionName_set(rhm010SessionTable_rowreq_ctx * rowreq_ctx,
                      char *rhm010SessionName_val_ptr,
                      size_t rhm010SessionName_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionName_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010SessionName_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010SessionName value.
     * set rhm010SessionName value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010SessionName, rhm010SessionName_val_ptr,
           rhm010SessionName_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010SessionName_len =
        rhm010SessionName_val_ptr_len /
        sizeof(rhm010SessionName_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010SessionName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010SessionName_undo(rhm010SessionTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010SessionTable:rhm010SessionName_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010SessionName undo.
     */
    /*
     * copy rhm010SessionName and rhm010SessionName_len data
     * set rowreq_ctx->data.rhm010SessionName from rowreq_ctx->undo->rhm010SessionName
     */
    memcpy(rowreq_ctx->data.rhm010SessionName,
           rowreq_ctx->undo->rhm010SessionName,
           (rowreq_ctx->undo->rhm010SessionName_len *
            sizeof(rowreq_ctx->data.rhm010SessionName[0])));
    rowreq_ctx->data.rhm010SessionName_len =
        rowreq_ctx->undo->rhm010SessionName_len;


    return MFD_SUCCESS;
}                               /* rhm010SessionName_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010SessionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010SessionTable if you don't have dependencies)
 */
int
rhm010SessionTable_check_dependencies(rhm010SessionTable_rowreq_ctx *
                                      rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010SessionTable:rhm010SessionTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010SessionTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010SessionTable_check_dependencies */

/** @} */
