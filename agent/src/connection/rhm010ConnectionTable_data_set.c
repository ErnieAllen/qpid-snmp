/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010ConnectionTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010ConnectionTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010ConnectionTable is subid 1 of rhm010Connections.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.10.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010ConnectionTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010ConnectionTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010ConnectionTable_undo_setup(rhm010ConnectionTable_rowreq_ctx *
                                 rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionTable_undo_setup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010ConnectionTable undo.
     * set up rhm010ConnectionTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010ConnectionMsgsToClient_val_ptr )*
     */

    return rc;
}                               /* rhm010ConnectionTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010ConnectionTable_undo(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010ConnectionTable undo.
     * rhm010ConnectionTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010ConnectionMsgsToClient_val_ptr )*
     */

    return rc;
}                               /* rhm010ConnectionTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ConnectionTable_undo_cleanup(rhm010ConnectionTable_rowreq_ctx *
                                   rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010ConnectionTable undo.
     * Undo storage is in (* rhm010ConnectionMsgsToClient_val_ptr )*
     */

    return rc;
}                               /* rhm010ConnectionTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ConnectionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010ConnectionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ConnectionTable_commit(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionTable_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010ConnectionTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010CONNECTIONVHOSTREF_FLAG) {
        save_flags &= ~COLUMN_RHM010CONNECTIONVHOSTREF_FLAG;    /* clear rhm010ConnectionVhostRef */
        /*
         * TODO:482:o: |-> commit column rhm010ConnectionVhostRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ConnectionTable column rhm010ConnectionVhostRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ConnectionVhostRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010CONNECTIONVHOSTREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010CONNECTIONADDRESS_FLAG) {
        save_flags &= ~COLUMN_RHM010CONNECTIONADDRESS_FLAG;     /* clear rhm010ConnectionAddress */
        /*
         * TODO:482:o: |-> commit column rhm010ConnectionAddress.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ConnectionTable column rhm010ConnectionAddress commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ConnectionAddress
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010CONNECTIONADDRESS_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010CONNECTIONINCOMING_FLAG) {
        save_flags &= ~COLUMN_RHM010CONNECTIONINCOMING_FLAG;    /* clear rhm010ConnectionIncoming */
        /*
         * TODO:482:o: |-> commit column rhm010ConnectionIncoming.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ConnectionTable column rhm010ConnectionIncoming commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ConnectionIncoming
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010CONNECTIONINCOMING_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010CONNECTIONSYSTEMCONNECTION_FLAG) {
        save_flags &= ~COLUMN_RHM010CONNECTIONSYSTEMCONNECTION_FLAG;    /* clear rhm010ConnectionSystemConnection */
        /*
         * TODO:482:o: |-> commit column rhm010ConnectionSystemConnection.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ConnectionTable column rhm010ConnectionSystemConnection commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ConnectionSystemConnection
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010CONNECTIONSYSTEMCONNECTION_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010ConnectionTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ConnectionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010ConnectionTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ConnectionTable_undo_commit(rhm010ConnectionTable_rowreq_ctx *
                                  rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010ConnectionTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010ConnectionTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010ConnectionTable node value checks.
 * TODO:450:M: Implement rhm010ConnectionTable undo functions.
 * TODO:460:M: Implement rhm010ConnectionTable set functions.
 * TODO:480:M: Implement rhm010ConnectionTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ConnectionEntry.rhm010ConnectionVhostRef
 * rhm010ConnectionVhostRef is subid 1 of rhm010ConnectionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.10.1.1.1
 * Description:
Connection vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ConnectionVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010ConnectionVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ConnectionVhostRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ConnectionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010ConnectionVhostRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ConnectionVhostRef_check_value(rhm010ConnectionTable_rowreq_ctx *
                                     rowreq_ctx,
                                     char
                                     *rhm010ConnectionVhostRef_val_ptr,
                                     size_t
                                     rhm010ConnectionVhostRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionVhostRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ConnectionVhostRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010ConnectionVhostRef value.
     */

    return MFD_SUCCESS;         /* rhm010ConnectionVhostRef value not illegal */
}                               /* rhm010ConnectionVhostRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ConnectionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ConnectionVhostRef_undo_setup(rhm010ConnectionTable_rowreq_ctx *
                                    rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionVhostRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ConnectionVhostRef undo.
     */
    /*
     * copy rhm010ConnectionVhostRef and rhm010ConnectionVhostRef_len data
     * set rowreq_ctx->undo->rhm010ConnectionVhostRef from rowreq_ctx->data.rhm010ConnectionVhostRef
     */
    memcpy(rowreq_ctx->undo->rhm010ConnectionVhostRef,
           rowreq_ctx->data.rhm010ConnectionVhostRef,
           (rowreq_ctx->data.rhm010ConnectionVhostRef_len *
            sizeof(rowreq_ctx->undo->rhm010ConnectionVhostRef[0])));
    rowreq_ctx->undo->rhm010ConnectionVhostRef_len =
        rowreq_ctx->data.rhm010ConnectionVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionVhostRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ConnectionVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010ConnectionVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ConnectionVhostRef_val_ptr
 */
int
rhm010ConnectionVhostRef_set(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx,
                             char *rhm010ConnectionVhostRef_val_ptr,
                             size_t rhm010ConnectionVhostRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionVhostRef_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ConnectionVhostRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010ConnectionVhostRef value.
     * set rhm010ConnectionVhostRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010ConnectionVhostRef,
           rhm010ConnectionVhostRef_val_ptr,
           rhm010ConnectionVhostRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010ConnectionVhostRef_len =
        rhm010ConnectionVhostRef_val_ptr_len /
        sizeof(rhm010ConnectionVhostRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010ConnectionVhostRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ConnectionVhostRef_undo(rhm010ConnectionTable_rowreq_ctx *
                              rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionVhostRef_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ConnectionVhostRef undo.
     */
    /*
     * copy rhm010ConnectionVhostRef and rhm010ConnectionVhostRef_len data
     * set rowreq_ctx->data.rhm010ConnectionVhostRef from rowreq_ctx->undo->rhm010ConnectionVhostRef
     */
    memcpy(rowreq_ctx->data.rhm010ConnectionVhostRef,
           rowreq_ctx->undo->rhm010ConnectionVhostRef,
           (rowreq_ctx->undo->rhm010ConnectionVhostRef_len *
            sizeof(rowreq_ctx->data.rhm010ConnectionVhostRef[0])));
    rowreq_ctx->data.rhm010ConnectionVhostRef_len =
        rowreq_ctx->undo->rhm010ConnectionVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionVhostRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ConnectionEntry.rhm010ConnectionAddress
 * rhm010ConnectionAddress is subid 2 of rhm010ConnectionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.10.1.1.2
 * Description:
Connection address
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ConnectionAddress_val_ptr
 *        A char containing the new value.
 * @param rhm010ConnectionAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ConnectionAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ConnectionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010ConnectionAddress).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ConnectionAddress_check_value(rhm010ConnectionTable_rowreq_ctx *
                                    rowreq_ctx,
                                    char *rhm010ConnectionAddress_val_ptr,
                                    size_t
                                    rhm010ConnectionAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionAddress_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ConnectionAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010ConnectionAddress value.
     */

    return MFD_SUCCESS;         /* rhm010ConnectionAddress value not illegal */
}                               /* rhm010ConnectionAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ConnectionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ConnectionAddress_undo_setup(rhm010ConnectionTable_rowreq_ctx *
                                   rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionAddress_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ConnectionAddress undo.
     */
    /*
     * copy rhm010ConnectionAddress and rhm010ConnectionAddress_len data
     * set rowreq_ctx->undo->rhm010ConnectionAddress from rowreq_ctx->data.rhm010ConnectionAddress
     */
    memcpy(rowreq_ctx->undo->rhm010ConnectionAddress,
           rowreq_ctx->data.rhm010ConnectionAddress,
           (rowreq_ctx->data.rhm010ConnectionAddress_len *
            sizeof(rowreq_ctx->undo->rhm010ConnectionAddress[0])));
    rowreq_ctx->undo->rhm010ConnectionAddress_len =
        rowreq_ctx->data.rhm010ConnectionAddress_len;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ConnectionAddress_val_ptr
 *        A char containing the new value.
 * @param rhm010ConnectionAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ConnectionAddress_val_ptr
 */
int
rhm010ConnectionAddress_set(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx,
                            char *rhm010ConnectionAddress_val_ptr,
                            size_t rhm010ConnectionAddress_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionAddress_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ConnectionAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010ConnectionAddress value.
     * set rhm010ConnectionAddress value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010ConnectionAddress,
           rhm010ConnectionAddress_val_ptr,
           rhm010ConnectionAddress_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010ConnectionAddress_len =
        rhm010ConnectionAddress_val_ptr_len /
        sizeof(rhm010ConnectionAddress_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010ConnectionAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ConnectionAddress_undo(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionAddress_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ConnectionAddress undo.
     */
    /*
     * copy rhm010ConnectionAddress and rhm010ConnectionAddress_len data
     * set rowreq_ctx->data.rhm010ConnectionAddress from rowreq_ctx->undo->rhm010ConnectionAddress
     */
    memcpy(rowreq_ctx->data.rhm010ConnectionAddress,
           rowreq_ctx->undo->rhm010ConnectionAddress,
           (rowreq_ctx->undo->rhm010ConnectionAddress_len *
            sizeof(rowreq_ctx->data.rhm010ConnectionAddress[0])));
    rowreq_ctx->data.rhm010ConnectionAddress_len =
        rowreq_ctx->undo->rhm010ConnectionAddress_len;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionAddress_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ConnectionEntry.rhm010ConnectionIncoming
 * rhm010ConnectionIncoming is subid 3 of rhm010ConnectionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.10.1.1.3
 * Description:
Connection incoming
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ConnectionIncoming_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ConnectionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ConnectionIncoming_check_value(rhm010ConnectionTable_rowreq_ctx *
                                     rowreq_ctx,
                                     u_long rhm010ConnectionIncoming_val)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionIncoming_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010ConnectionIncoming value.
     */

    return MFD_SUCCESS;         /* rhm010ConnectionIncoming value not illegal */
}                               /* rhm010ConnectionIncoming_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ConnectionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ConnectionIncoming_undo_setup(rhm010ConnectionTable_rowreq_ctx *
                                    rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionIncoming_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ConnectionIncoming undo.
     */
    /*
     * copy rhm010ConnectionIncoming data
     * set rowreq_ctx->undo->rhm010ConnectionIncoming from rowreq_ctx->data.rhm010ConnectionIncoming
     */
    rowreq_ctx->undo->rhm010ConnectionIncoming =
        rowreq_ctx->data.rhm010ConnectionIncoming;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionIncoming_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ConnectionIncoming_val
 *        A long containing the new value.
 */
int
rhm010ConnectionIncoming_set(rhm010ConnectionTable_rowreq_ctx * rowreq_ctx,
                             u_long rhm010ConnectionIncoming_val)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionIncoming_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010ConnectionIncoming value.
     * set rhm010ConnectionIncoming value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010ConnectionIncoming =
        rhm010ConnectionIncoming_val;

    return MFD_SUCCESS;
}                               /* rhm010ConnectionIncoming_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ConnectionIncoming_undo(rhm010ConnectionTable_rowreq_ctx *
                              rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionIncoming_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ConnectionIncoming undo.
     */
    /*
     * copy rhm010ConnectionIncoming data
     * set rowreq_ctx->data.rhm010ConnectionIncoming from rowreq_ctx->undo->rhm010ConnectionIncoming
     */
    rowreq_ctx->data.rhm010ConnectionIncoming =
        rowreq_ctx->undo->rhm010ConnectionIncoming;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionIncoming_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ConnectionEntry.rhm010ConnectionSystemConnection
 * rhm010ConnectionSystemConnection is subid 4 of rhm010ConnectionEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.10.1.1.4
 * Description:
Infrastructure/ Inter-system connection (Cluster, Federation, ...)
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ConnectionSystemConnection_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ConnectionTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ConnectionSystemConnection_check_value
    (rhm010ConnectionTable_rowreq_ctx * rowreq_ctx,
     u_long rhm010ConnectionSystemConnection_val)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionSystemConnection_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010ConnectionSystemConnection value.
     */

    return MFD_SUCCESS;         /* rhm010ConnectionSystemConnection value not illegal */
}                               /* rhm010ConnectionSystemConnection_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ConnectionTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ConnectionTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ConnectionSystemConnection_undo_setup
    (rhm010ConnectionTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionSystemConnection_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ConnectionSystemConnection undo.
     */
    /*
     * copy rhm010ConnectionSystemConnection data
     * set rowreq_ctx->undo->rhm010ConnectionSystemConnection from rowreq_ctx->data.rhm010ConnectionSystemConnection
     */
    rowreq_ctx->undo->rhm010ConnectionSystemConnection =
        rowreq_ctx->data.rhm010ConnectionSystemConnection;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionSystemConnection_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ConnectionSystemConnection_val
 *        A long containing the new value.
 */
int
rhm010ConnectionSystemConnection_set(rhm010ConnectionTable_rowreq_ctx *
                                     rowreq_ctx,
                                     u_long
                                     rhm010ConnectionSystemConnection_val)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionSystemConnection_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010ConnectionSystemConnection value.
     * set rhm010ConnectionSystemConnection value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010ConnectionSystemConnection =
        rhm010ConnectionSystemConnection_val;

    return MFD_SUCCESS;
}                               /* rhm010ConnectionSystemConnection_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ConnectionSystemConnection_undo(rhm010ConnectionTable_rowreq_ctx *
                                      rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ConnectionTable:rhm010ConnectionSystemConnection_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ConnectionSystemConnection undo.
     */
    /*
     * copy rhm010ConnectionSystemConnection data
     * set rowreq_ctx->data.rhm010ConnectionSystemConnection from rowreq_ctx->undo->rhm010ConnectionSystemConnection
     */
    rowreq_ctx->data.rhm010ConnectionSystemConnection =
        rowreq_ctx->undo->rhm010ConnectionSystemConnection;


    return MFD_SUCCESS;
}                               /* rhm010ConnectionSystemConnection_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ConnectionTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010ConnectionTable if you don't have dependencies)
 */
int
rhm010ConnectionTable_check_dependencies(rhm010ConnectionTable_rowreq_ctx *
                                         rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010ConnectionTable:rhm010ConnectionTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010ConnectionTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010ConnectionTable_check_dependencies */

/** @} */
