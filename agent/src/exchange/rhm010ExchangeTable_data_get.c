/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010ExchangeTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement rhm010ExchangeTable get routines.
 * TODO:240:M: Implement rhm010ExchangeTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010ExchangeTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010ExchangeTable is subid 1 of rhm010Exchanges.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1, length: 12
 */

/*
 * ---------------------------------------------------------------------
 * * TODO:200:r: Implement rhm010ExchangeTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param rhm010ExchangeInternalIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
rhm010ExchangeTable_indexes_set_tbl_idx(rhm010ExchangeTable_mib_index *
                                        tbl_idx,
                                        u_long
                                        rhm010ExchangeInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_indexes_set_tbl_idx", "called\n"));

    /*
     * rhm010ExchangeInternalIndex(20)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/r/d/h 
     */
    tbl_idx->rhm010ExchangeInternalIndex = rhm010ExchangeInternalIndex_val;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
rhm010ExchangeTable_indexes_set(rhm010ExchangeTable_rowreq_ctx *
                                rowreq_ctx,
                                u_long rhm010ExchangeInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_indexes_set", "called\n"));

    if (MFD_SUCCESS !=
        rhm010ExchangeTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx,
                                                rhm010ExchangeInternalIndex_val))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if (0 != rhm010ExchangeTable_index_to_oid(&rowreq_ctx->oid_idx,
                                              &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
}                               /* rhm010ExchangeTable_indexes_set */


/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeVhostRef
 * rhm010ExchangeVhostRef is subid 1 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.1
 * Description:
Exchange vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010ExchangeVhostRef data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeVhostRef_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010ExchangeVhostRef_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010ExchangeVhostRef.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010ExchangeVhostRef_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010ExchangeVhostRef_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010ExchangeVhostRef_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                           char **rhm010ExchangeVhostRef_val_ptr_ptr,
                           size_t *rhm010ExchangeVhostRef_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010ExchangeVhostRef_val_ptr_ptr)
                   && (NULL != *rhm010ExchangeVhostRef_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010ExchangeVhostRef_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeVhostRef_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeVhostRef data.
     * copy (* rhm010ExchangeVhostRef_val_ptr_ptr ) data and (* rhm010ExchangeVhostRef_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010ExchangeVhostRef data
     */
    if ((NULL == (*rhm010ExchangeVhostRef_val_ptr_ptr)) ||
        ((*rhm010ExchangeVhostRef_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010ExchangeVhostRef_len *
          sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef[0])))) {
        /*
         * allocate space for rhm010ExchangeVhostRef data
         */
        (*rhm010ExchangeVhostRef_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010ExchangeVhostRef_len *
                   sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef[0]));
        if (NULL == (*rhm010ExchangeVhostRef_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010ExchangeVhostRef)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010ExchangeVhostRef_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010ExchangeVhostRef_len *
        sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef[0]);
    memcpy((*rhm010ExchangeVhostRef_val_ptr_ptr),
           rowreq_ctx->data.rhm010ExchangeVhostRef,
           rowreq_ctx->data.rhm010ExchangeVhostRef_len *
           sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef[0]));

    return MFD_SUCCESS;
}                               /* rhm010ExchangeVhostRef_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeName
 * rhm010ExchangeName is subid 2 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.2
 * Description:
Exchange name
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010ExchangeName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010ExchangeName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010ExchangeName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010ExchangeName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010ExchangeName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010ExchangeName_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                       char **rhm010ExchangeName_val_ptr_ptr,
                       size_t *rhm010ExchangeName_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010ExchangeName_val_ptr_ptr)
                   && (NULL != *rhm010ExchangeName_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010ExchangeName_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeName_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeName data.
     * copy (* rhm010ExchangeName_val_ptr_ptr ) data and (* rhm010ExchangeName_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010ExchangeName data
     */
    if ((NULL == (*rhm010ExchangeName_val_ptr_ptr)) ||
        ((*rhm010ExchangeName_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010ExchangeName_len *
          sizeof(rowreq_ctx->data.rhm010ExchangeName[0])))) {
        /*
         * allocate space for rhm010ExchangeName data
         */
        (*rhm010ExchangeName_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010ExchangeName_len *
                   sizeof(rowreq_ctx->data.rhm010ExchangeName[0]));
        if (NULL == (*rhm010ExchangeName_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010ExchangeName)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010ExchangeName_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010ExchangeName_len *
        sizeof(rowreq_ctx->data.rhm010ExchangeName[0]);
    memcpy((*rhm010ExchangeName_val_ptr_ptr),
           rowreq_ctx->data.rhm010ExchangeName,
           rowreq_ctx->data.rhm010ExchangeName_len *
           sizeof(rowreq_ctx->data.rhm010ExchangeName[0]));

    return MFD_SUCCESS;
}                               /* rhm010ExchangeName_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeType
 * rhm010ExchangeType is subid 3 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.3
 * Description:
Exchange type
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010ExchangeType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeType_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010ExchangeType_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010ExchangeType.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010ExchangeType_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010ExchangeType_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010ExchangeType_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                       char **rhm010ExchangeType_val_ptr_ptr,
                       size_t *rhm010ExchangeType_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010ExchangeType_val_ptr_ptr)
                   && (NULL != *rhm010ExchangeType_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010ExchangeType_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeType_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeType data.
     * copy (* rhm010ExchangeType_val_ptr_ptr ) data and (* rhm010ExchangeType_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010ExchangeType data
     */
    if ((NULL == (*rhm010ExchangeType_val_ptr_ptr)) ||
        ((*rhm010ExchangeType_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010ExchangeType_len *
          sizeof(rowreq_ctx->data.rhm010ExchangeType[0])))) {
        /*
         * allocate space for rhm010ExchangeType data
         */
        (*rhm010ExchangeType_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010ExchangeType_len *
                   sizeof(rowreq_ctx->data.rhm010ExchangeType[0]));
        if (NULL == (*rhm010ExchangeType_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010ExchangeType)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010ExchangeType_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010ExchangeType_len *
        sizeof(rowreq_ctx->data.rhm010ExchangeType[0]);
    memcpy((*rhm010ExchangeType_val_ptr_ptr),
           rowreq_ctx->data.rhm010ExchangeType,
           rowreq_ctx->data.rhm010ExchangeType_len *
           sizeof(rowreq_ctx->data.rhm010ExchangeType[0]));

    return MFD_SUCCESS;
}                               /* rhm010ExchangeType_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeDurable
 * rhm010ExchangeDurable is subid 4 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.4
 * Description:
Exchange durable
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the rhm010ExchangeDurable data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeDurable_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeDurable_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                          u_long * rhm010ExchangeDurable_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeDurable_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeDurable_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeDurable data.
     * copy (* rhm010ExchangeDurable_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeDurable_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeDurable;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeDurable_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeAutoDelete
 * rhm010ExchangeAutoDelete is subid 5 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.5
 * Description:
Exchange autoDelete
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the rhm010ExchangeAutoDelete data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeAutoDelete_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeAutoDelete_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                             u_long * rhm010ExchangeAutoDelete_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeAutoDelete_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeAutoDelete_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeAutoDelete data.
     * copy (* rhm010ExchangeAutoDelete_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeAutoDelete_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeAutoDelete;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeAutoDelete_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeAltExchange
 * rhm010ExchangeAltExchange is subid 6 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.6
 * Description:
Exchange altExchange
                     Additional info ( nodeType:property, references:Exchange, optional:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010ExchangeAltExchange data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeAltExchange_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010ExchangeAltExchange_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010ExchangeAltExchange.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010ExchangeAltExchange_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010ExchangeAltExchange_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010ExchangeAltExchange_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                              char **rhm010ExchangeAltExchange_val_ptr_ptr,
                              size_t
                              *rhm010ExchangeAltExchange_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010ExchangeAltExchange_val_ptr_ptr)
                   && (NULL != *rhm010ExchangeAltExchange_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010ExchangeAltExchange_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeAltExchange_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeAltExchange data.
     * copy (* rhm010ExchangeAltExchange_val_ptr_ptr ) data and (* rhm010ExchangeAltExchange_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010ExchangeAltExchange data
     */
    if ((NULL == (*rhm010ExchangeAltExchange_val_ptr_ptr)) ||
        ((*rhm010ExchangeAltExchange_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010ExchangeAltExchange_len *
          sizeof(rowreq_ctx->data.rhm010ExchangeAltExchange[0])))) {
        /*
         * allocate space for rhm010ExchangeAltExchange data
         */
        (*rhm010ExchangeAltExchange_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010ExchangeAltExchange_len *
                   sizeof(rowreq_ctx->data.rhm010ExchangeAltExchange[0]));
        if (NULL == (*rhm010ExchangeAltExchange_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010ExchangeAltExchange)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010ExchangeAltExchange_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010ExchangeAltExchange_len *
        sizeof(rowreq_ctx->data.rhm010ExchangeAltExchange[0]);
    memcpy((*rhm010ExchangeAltExchange_val_ptr_ptr),
           rowreq_ctx->data.rhm010ExchangeAltExchange,
           rowreq_ctx->data.rhm010ExchangeAltExchange_len *
           sizeof(rowreq_ctx->data.rhm010ExchangeAltExchange[0]));

    return MFD_SUCCESS;
}                               /* rhm010ExchangeAltExchange_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeArguments
 * rhm010ExchangeArguments is subid 7 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.7
 * Description:
Arguments supplied in exchange.declare
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Map (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Extract the current value of the rhm010ExchangeArguments data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeArguments_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010ExchangeArguments_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010ExchangeArguments.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010ExchangeArguments_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010ExchangeArguments_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010ExchangeArguments_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                            char **rhm010ExchangeArguments_val_ptr_ptr,
                            size_t
                            *rhm010ExchangeArguments_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010ExchangeArguments_val_ptr_ptr)
                   && (NULL != *rhm010ExchangeArguments_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010ExchangeArguments_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeArguments_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeArguments data.
     * copy (* rhm010ExchangeArguments_val_ptr_ptr ) data and (* rhm010ExchangeArguments_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010ExchangeArguments data
     */
    if ((NULL == (*rhm010ExchangeArguments_val_ptr_ptr)) ||
        ((*rhm010ExchangeArguments_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010ExchangeArguments_len *
          sizeof(rowreq_ctx->data.rhm010ExchangeArguments[0])))) {
        /*
         * allocate space for rhm010ExchangeArguments data
         */
        (*rhm010ExchangeArguments_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010ExchangeArguments_len *
                   sizeof(rowreq_ctx->data.rhm010ExchangeArguments[0]));
        if (NULL == (*rhm010ExchangeArguments_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010ExchangeArguments)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010ExchangeArguments_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010ExchangeArguments_len *
        sizeof(rowreq_ctx->data.rhm010ExchangeArguments[0]);
    memcpy((*rhm010ExchangeArguments_val_ptr_ptr),
           rowreq_ctx->data.rhm010ExchangeArguments,
           rowreq_ctx->data.rhm010ExchangeArguments_len *
           sizeof(rowreq_ctx->data.rhm010ExchangeArguments[0]));

    return MFD_SUCCESS;
}                               /* rhm010ExchangeArguments_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeProducerCount
 * rhm010ExchangeProducerCount is subid 8 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.8
 * Description:
Current producers on exchange
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeProducerCount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeProducerCount_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeProducerCount_get(rhm010ExchangeTable_rowreq_ctx *
                                rowreq_ctx,
                                long *rhm010ExchangeProducerCount_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeProducerCount_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeProducerCount_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeProducerCount data.
     * copy (* rhm010ExchangeProducerCount_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeProducerCount_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeProducerCount;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeProducerCount_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeProducerCountHi
 * rhm010ExchangeProducerCountHi is subid 9 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.9
 * Description:
Current producers on exchange
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeProducerCountHi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeProducerCountHi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeProducerCountHi_get(rhm010ExchangeTable_rowreq_ctx *
                                  rowreq_ctx,
                                  long
                                  *rhm010ExchangeProducerCountHi_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeProducerCountHi_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeProducerCountHi_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeProducerCountHi data.
     * copy (* rhm010ExchangeProducerCountHi_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeProducerCountHi_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeProducerCountHi;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeProducerCountHi_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeProducerCountLow
 * rhm010ExchangeProducerCountLow is subid 10 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.10
 * Description:
Current producers on exchange
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeProducerCountLow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeProducerCountLow_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeProducerCountLow_get(rhm010ExchangeTable_rowreq_ctx *
                                   rowreq_ctx,
                                   long
                                   *rhm010ExchangeProducerCountLow_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeProducerCountLow_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeProducerCountLow_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeProducerCountLow data.
     * copy (* rhm010ExchangeProducerCountLow_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeProducerCountLow_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeProducerCountLow;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeProducerCountLow_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeBindingCount
 * rhm010ExchangeBindingCount is subid 11 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.11
 * Description:
Current bindings
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeBindingCount data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeBindingCount_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeBindingCount_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                               long *rhm010ExchangeBindingCount_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeBindingCount_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeBindingCount_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeBindingCount data.
     * copy (* rhm010ExchangeBindingCount_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeBindingCount_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeBindingCount;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeBindingCount_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeBindingCountHi
 * rhm010ExchangeBindingCountHi is subid 12 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.12
 * Description:
Current bindings
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeBindingCountHi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeBindingCountHi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeBindingCountHi_get(rhm010ExchangeTable_rowreq_ctx *
                                 rowreq_ctx,
                                 long
                                 *rhm010ExchangeBindingCountHi_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeBindingCountHi_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeBindingCountHi_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeBindingCountHi data.
     * copy (* rhm010ExchangeBindingCountHi_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeBindingCountHi_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeBindingCountHi;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeBindingCountHi_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeBindingCountLow
 * rhm010ExchangeBindingCountLow is subid 13 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.13
 * Description:
Current bindings
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   0
 *   hint: d
 *
 *
 * Its syntax is Hilo32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010ExchangeBindingCountLow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeBindingCountLow_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeBindingCountLow_get(rhm010ExchangeTable_rowreq_ctx *
                                  rowreq_ctx,
                                  long
                                  *rhm010ExchangeBindingCountLow_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeBindingCountLow_val_ptr);


    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeBindingCountLow_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010ExchangeBindingCountLow data.
     * copy (* rhm010ExchangeBindingCountLow_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010ExchangeBindingCountLow_val_ptr) =
        rowreq_ctx->data.rhm010ExchangeBindingCountLow;

    return MFD_SUCCESS;
}                               /* rhm010ExchangeBindingCountLow_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeMsgReceives
 * rhm010ExchangeMsgReceives is subid 14 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.14
 * Description:
Total messages received
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeMsgReceives data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeMsgReceives_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeMsgReceives_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                              U64 * rhm010ExchangeMsgReceives_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeMsgReceives_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeMsgReceives data.
     * get (* rhm010ExchangeMsgReceives_val_ptr ).low and (* rhm010ExchangeMsgReceives_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeMsgReceives_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeMsgReceives.high;
    (*rhm010ExchangeMsgReceives_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeMsgReceives.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeMsgReceives_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeMsgDrops
 * rhm010ExchangeMsgDrops is subid 15 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.15
 * Description:
Total messages dropped (no matching key)
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeMsgDrops data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeMsgDrops_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeMsgDrops_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                           U64 * rhm010ExchangeMsgDrops_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeMsgDrops_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeMsgDrops data.
     * get (* rhm010ExchangeMsgDrops_val_ptr ).low and (* rhm010ExchangeMsgDrops_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeMsgDrops_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeMsgDrops.high;
    (*rhm010ExchangeMsgDrops_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeMsgDrops.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeMsgDrops_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeMsgRoutes
 * rhm010ExchangeMsgRoutes is subid 16 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.16
 * Description:
Total routed messages
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeMsgRoutes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeMsgRoutes_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeMsgRoutes_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                            U64 * rhm010ExchangeMsgRoutes_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeMsgRoutes_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeMsgRoutes data.
     * get (* rhm010ExchangeMsgRoutes_val_ptr ).low and (* rhm010ExchangeMsgRoutes_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeMsgRoutes_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeMsgRoutes.high;
    (*rhm010ExchangeMsgRoutes_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeMsgRoutes.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeMsgRoutes_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeByteReceives
 * rhm010ExchangeByteReceives is subid 17 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.17
 * Description:
Total bytes received
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeByteReceives data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeByteReceives_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeByteReceives_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                               U64 * rhm010ExchangeByteReceives_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeByteReceives_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeByteReceives data.
     * get (* rhm010ExchangeByteReceives_val_ptr ).low and (* rhm010ExchangeByteReceives_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeByteReceives_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeByteReceives.high;
    (*rhm010ExchangeByteReceives_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeByteReceives.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeByteReceives_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeByteDrops
 * rhm010ExchangeByteDrops is subid 18 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.18
 * Description:
Total bytes dropped (no matching key)
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeByteDrops data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeByteDrops_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeByteDrops_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                            U64 * rhm010ExchangeByteDrops_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeByteDrops_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeByteDrops data.
     * get (* rhm010ExchangeByteDrops_val_ptr ).low and (* rhm010ExchangeByteDrops_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeByteDrops_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeByteDrops.high;
    (*rhm010ExchangeByteDrops_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeByteDrops.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeByteDrops_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeByteRoutes
 * rhm010ExchangeByteRoutes is subid 19 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.19
 * Description:
Total routed bytes
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010ExchangeByteRoutes data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeByteRoutes_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010ExchangeByteRoutes_get(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                             U64 * rhm010ExchangeByteRoutes_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010ExchangeByteRoutes_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010ExchangeByteRoutes data.
     * get (* rhm010ExchangeByteRoutes_val_ptr ).low and (* rhm010ExchangeByteRoutes_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010ExchangeByteRoutes_val_ptr).high =
        rowreq_ctx->data.rhm010ExchangeByteRoutes.high;
    (*rhm010ExchangeByteRoutes_val_ptr).low =
        rowreq_ctx->data.rhm010ExchangeByteRoutes.low;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeByteRoutes_get */



/** @} */
