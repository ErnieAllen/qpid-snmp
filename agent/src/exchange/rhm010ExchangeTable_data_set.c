/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010ExchangeTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010ExchangeTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010ExchangeTable is subid 1 of rhm010Exchanges.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010ExchangeTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010ExchangeTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ExchangeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010ExchangeTable_undo_setup(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_undo_setup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010ExchangeTable undo.
     * set up rhm010ExchangeTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010ExchangeByteRoutes_val_ptr )*
     */

    return rc;
}                               /* rhm010ExchangeTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ExchangeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010ExchangeTable_undo(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010ExchangeTable undo.
     * rhm010ExchangeTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010ExchangeByteRoutes_val_ptr )*
     */

    return rc;
}                               /* rhm010ExchangeTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ExchangeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ExchangeTable_undo_cleanup(rhm010ExchangeTable_rowreq_ctx *
                                 rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010ExchangeTable undo.
     * Undo storage is in (* rhm010ExchangeByteRoutes_val_ptr )*
     */

    return rc;
}                               /* rhm010ExchangeTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ExchangeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010ExchangeTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ExchangeTable_commit(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010ExchangeTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010EXCHANGEVHOSTREF_FLAG) {
        save_flags &= ~COLUMN_RHM010EXCHANGEVHOSTREF_FLAG;      /* clear rhm010ExchangeVhostRef */
        /*
         * TODO:482:o: |-> commit column rhm010ExchangeVhostRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ExchangeTable column rhm010ExchangeVhostRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ExchangeVhostRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010EXCHANGEVHOSTREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010EXCHANGENAME_FLAG) {
        save_flags &= ~COLUMN_RHM010EXCHANGENAME_FLAG;  /* clear rhm010ExchangeName */
        /*
         * TODO:482:o: |-> commit column rhm010ExchangeName.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010ExchangeTable column rhm010ExchangeName commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010ExchangeName
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010EXCHANGENAME_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010ExchangeTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ExchangeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010ExchangeTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010ExchangeTable_undo_commit(rhm010ExchangeTable_rowreq_ctx *
                                rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010ExchangeTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010ExchangeTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010ExchangeTable node value checks.
 * TODO:450:M: Implement rhm010ExchangeTable undo functions.
 * TODO:460:M: Implement rhm010ExchangeTable set functions.
 * TODO:480:M: Implement rhm010ExchangeTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeVhostRef
 * rhm010ExchangeVhostRef is subid 1 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.1
 * Description:
Exchange vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010ExchangeVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ExchangeVhostRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ExchangeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ExchangeVhostRef_check_value(rhm010ExchangeTable_rowreq_ctx *
                                   rowreq_ctx,
                                   char *rhm010ExchangeVhostRef_val_ptr,
                                   size_t
                                   rhm010ExchangeVhostRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeVhostRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ExchangeVhostRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010ExchangeVhostRef value.
     */

    return MFD_SUCCESS;         /* rhm010ExchangeVhostRef value not illegal */
}                               /* rhm010ExchangeVhostRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ExchangeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ExchangeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ExchangeVhostRef_undo_setup(rhm010ExchangeTable_rowreq_ctx *
                                  rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeVhostRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ExchangeVhostRef undo.
     */
    /*
     * copy rhm010ExchangeVhostRef and rhm010ExchangeVhostRef_len data
     * set rowreq_ctx->undo->rhm010ExchangeVhostRef from rowreq_ctx->data.rhm010ExchangeVhostRef
     */
    memcpy(rowreq_ctx->undo->rhm010ExchangeVhostRef,
           rowreq_ctx->data.rhm010ExchangeVhostRef,
           (rowreq_ctx->data.rhm010ExchangeVhostRef_len *
            sizeof(rowreq_ctx->undo->rhm010ExchangeVhostRef[0])));
    rowreq_ctx->undo->rhm010ExchangeVhostRef_len =
        rowreq_ctx->data.rhm010ExchangeVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeVhostRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ExchangeVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010ExchangeVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ExchangeVhostRef_val_ptr
 */
int
rhm010ExchangeVhostRef_set(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                           char *rhm010ExchangeVhostRef_val_ptr,
                           size_t rhm010ExchangeVhostRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeVhostRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ExchangeVhostRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010ExchangeVhostRef value.
     * set rhm010ExchangeVhostRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010ExchangeVhostRef,
           rhm010ExchangeVhostRef_val_ptr,
           rhm010ExchangeVhostRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010ExchangeVhostRef_len =
        rhm010ExchangeVhostRef_val_ptr_len /
        sizeof(rhm010ExchangeVhostRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010ExchangeVhostRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ExchangeVhostRef_undo(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeVhostRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ExchangeVhostRef undo.
     */
    /*
     * copy rhm010ExchangeVhostRef and rhm010ExchangeVhostRef_len data
     * set rowreq_ctx->data.rhm010ExchangeVhostRef from rowreq_ctx->undo->rhm010ExchangeVhostRef
     */
    memcpy(rowreq_ctx->data.rhm010ExchangeVhostRef,
           rowreq_ctx->undo->rhm010ExchangeVhostRef,
           (rowreq_ctx->undo->rhm010ExchangeVhostRef_len *
            sizeof(rowreq_ctx->data.rhm010ExchangeVhostRef[0])));
    rowreq_ctx->data.rhm010ExchangeVhostRef_len =
        rowreq_ctx->undo->rhm010ExchangeVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeVhostRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010ExchangeEntry.rhm010ExchangeName
 * rhm010ExchangeName is subid 2 of rhm010ExchangeEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.7.1.1.2
 * Description:
Exchange name
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010ExchangeName_val_ptr
 *        A char containing the new value.
 * @param rhm010ExchangeName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ExchangeName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010ExchangeTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010ExchangeName).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010ExchangeName_check_value(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                               char *rhm010ExchangeName_val_ptr,
                               size_t rhm010ExchangeName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeName_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ExchangeName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010ExchangeName value.
     */

    return MFD_SUCCESS;         /* rhm010ExchangeName value not illegal */
}                               /* rhm010ExchangeName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010ExchangeTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010ExchangeTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010ExchangeName_undo_setup(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeName_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010ExchangeName undo.
     */
    /*
     * copy rhm010ExchangeName and rhm010ExchangeName_len data
     * set rowreq_ctx->undo->rhm010ExchangeName from rowreq_ctx->data.rhm010ExchangeName
     */
    memcpy(rowreq_ctx->undo->rhm010ExchangeName,
           rowreq_ctx->data.rhm010ExchangeName,
           (rowreq_ctx->data.rhm010ExchangeName_len *
            sizeof(rowreq_ctx->undo->rhm010ExchangeName[0])));
    rowreq_ctx->undo->rhm010ExchangeName_len =
        rowreq_ctx->data.rhm010ExchangeName_len;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010ExchangeName_val_ptr
 *        A char containing the new value.
 * @param rhm010ExchangeName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010ExchangeName_val_ptr
 */
int
rhm010ExchangeName_set(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx,
                       char *rhm010ExchangeName_val_ptr,
                       size_t rhm010ExchangeName_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeName_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010ExchangeName_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010ExchangeName value.
     * set rhm010ExchangeName value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010ExchangeName, rhm010ExchangeName_val_ptr,
           rhm010ExchangeName_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010ExchangeName_len =
        rhm010ExchangeName_val_ptr_len /
        sizeof(rhm010ExchangeName_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010ExchangeName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010ExchangeName_undo(rhm010ExchangeTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010ExchangeTable:rhm010ExchangeName_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010ExchangeName undo.
     */
    /*
     * copy rhm010ExchangeName and rhm010ExchangeName_len data
     * set rowreq_ctx->data.rhm010ExchangeName from rowreq_ctx->undo->rhm010ExchangeName
     */
    memcpy(rowreq_ctx->data.rhm010ExchangeName,
           rowreq_ctx->undo->rhm010ExchangeName,
           (rowreq_ctx->undo->rhm010ExchangeName_len *
            sizeof(rowreq_ctx->data.rhm010ExchangeName[0])));
    rowreq_ctx->data.rhm010ExchangeName_len =
        rowreq_ctx->undo->rhm010ExchangeName_len;


    return MFD_SUCCESS;
}                               /* rhm010ExchangeName_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010ExchangeTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010ExchangeTable if you don't have dependencies)
 */
int
rhm010ExchangeTable_check_dependencies(rhm010ExchangeTable_rowreq_ctx *
                                       rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010ExchangeTable:rhm010ExchangeTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010ExchangeTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010ExchangeTable_check_dependencies */

/** @} */
