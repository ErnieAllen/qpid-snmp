/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010LinkTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010LinkTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010LinkTable is subid 1 of rhm010Links.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010LinkTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010LinkTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010LinkTable_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_undo_setup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010LinkTable undo.
     * set up rhm010LinkTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010LinkLastError_val_ptr_ptr )*
     */

    return rc;
}                               /* rhm010LinkTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010LinkTable_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010LinkTable undo.
     * rhm010LinkTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010LinkLastError_val_ptr_ptr )*
     */

    return rc;
}                               /* rhm010LinkTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010LinkTable_undo_cleanup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_undo_cleanup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010LinkTable undo.
     * Undo storage is in (* rhm010LinkLastError_val_ptr_ptr )*
     */

    return rc;
}                               /* rhm010LinkTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010LinkTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010LinkTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010LinkTable_commit(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010LinkTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010LINKVHOSTREF_FLAG) {
        save_flags &= ~COLUMN_RHM010LINKVHOSTREF_FLAG;  /* clear rhm010LinkVhostRef */
        /*
         * TODO:482:o: |-> commit column rhm010LinkVhostRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010LinkTable column rhm010LinkVhostRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010LinkVhostRef
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010LINKVHOSTREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010LINKHOST_FLAG) {
        save_flags &= ~COLUMN_RHM010LINKHOST_FLAG;      /* clear rhm010LinkHost */
        /*
         * TODO:482:o: |-> commit column rhm010LinkHost.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010LinkTable column rhm010LinkHost commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010LinkHost
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010LINKHOST_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010LINKPORT_FLAG) {
        save_flags &= ~COLUMN_RHM010LINKPORT_FLAG;      /* clear rhm010LinkPort */
        /*
         * TODO:482:o: |-> commit column rhm010LinkPort.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010LinkTable column rhm010LinkPort commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010LinkPort
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010LINKPORT_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010LINKTRANSPORT_FLAG) {
        save_flags &= ~COLUMN_RHM010LINKTRANSPORT_FLAG; /* clear rhm010LinkTransport */
        /*
         * TODO:482:o: |-> commit column rhm010LinkTransport.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010LinkTable column rhm010LinkTransport commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010LinkTransport
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010LINKTRANSPORT_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010LINKDURABLE_FLAG) {
        save_flags &= ~COLUMN_RHM010LINKDURABLE_FLAG;   /* clear rhm010LinkDurable */
        /*
         * TODO:482:o: |-> commit column rhm010LinkDurable.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010LinkTable column rhm010LinkDurable commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010LinkDurable
             */
            rowreq_ctx->column_set_flags |= COLUMN_RHM010LINKDURABLE_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010LinkTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010LinkTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010LinkTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010LinkTable_undo_commit(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_undo_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010LinkTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010LinkTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010LinkTable node value checks.
 * TODO:450:M: Implement rhm010LinkTable undo functions.
 * TODO:460:M: Implement rhm010LinkTable set functions.
 * TODO:480:M: Implement rhm010LinkTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkVhostRef
 * rhm010LinkVhostRef is subid 1 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.1
 * Description:
Link vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkVhostRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010LinkTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010LinkVhostRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010LinkVhostRef_check_value(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                               char *rhm010LinkVhostRef_val_ptr,
                               size_t rhm010LinkVhostRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkVhostRef_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkVhostRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010LinkVhostRef value.
     */

    return MFD_SUCCESS;         /* rhm010LinkVhostRef value not illegal */
}                               /* rhm010LinkVhostRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010LinkTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010LinkVhostRef_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkVhostRef_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010LinkVhostRef undo.
     */
    /*
     * copy rhm010LinkVhostRef and rhm010LinkVhostRef_len data
     * set rowreq_ctx->undo->rhm010LinkVhostRef from rowreq_ctx->data.rhm010LinkVhostRef
     */
    memcpy(rowreq_ctx->undo->rhm010LinkVhostRef,
           rowreq_ctx->data.rhm010LinkVhostRef,
           (rowreq_ctx->data.rhm010LinkVhostRef_len *
            sizeof(rowreq_ctx->undo->rhm010LinkVhostRef[0])));
    rowreq_ctx->undo->rhm010LinkVhostRef_len =
        rowreq_ctx->data.rhm010LinkVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkVhostRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010LinkVhostRef_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkVhostRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkVhostRef_val_ptr
 */
int
rhm010LinkVhostRef_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                       char *rhm010LinkVhostRef_val_ptr,
                       size_t rhm010LinkVhostRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkVhostRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkVhostRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010LinkVhostRef value.
     * set rhm010LinkVhostRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010LinkVhostRef, rhm010LinkVhostRef_val_ptr,
           rhm010LinkVhostRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010LinkVhostRef_len =
        rhm010LinkVhostRef_val_ptr_len /
        sizeof(rhm010LinkVhostRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010LinkVhostRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010LinkVhostRef_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkVhostRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010LinkVhostRef undo.
     */
    /*
     * copy rhm010LinkVhostRef and rhm010LinkVhostRef_len data
     * set rowreq_ctx->data.rhm010LinkVhostRef from rowreq_ctx->undo->rhm010LinkVhostRef
     */
    memcpy(rowreq_ctx->data.rhm010LinkVhostRef,
           rowreq_ctx->undo->rhm010LinkVhostRef,
           (rowreq_ctx->undo->rhm010LinkVhostRef_len *
            sizeof(rowreq_ctx->data.rhm010LinkVhostRef[0])));
    rowreq_ctx->data.rhm010LinkVhostRef_len =
        rowreq_ctx->undo->rhm010LinkVhostRef_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkVhostRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkHost
 * rhm010LinkHost is subid 2 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.2
 * Description:
Link host
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkHost_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkHost_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkHost_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010LinkTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010LinkHost).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010LinkHost_check_value(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                           char *rhm010LinkHost_val_ptr,
                           size_t rhm010LinkHost_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkHost_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkHost_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010LinkHost value.
     */

    return MFD_SUCCESS;         /* rhm010LinkHost value not illegal */
}                               /* rhm010LinkHost_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010LinkTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010LinkHost_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkHost_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010LinkHost undo.
     */
    /*
     * copy rhm010LinkHost and rhm010LinkHost_len data
     * set rowreq_ctx->undo->rhm010LinkHost from rowreq_ctx->data.rhm010LinkHost
     */
    memcpy(rowreq_ctx->undo->rhm010LinkHost,
           rowreq_ctx->data.rhm010LinkHost,
           (rowreq_ctx->data.rhm010LinkHost_len *
            sizeof(rowreq_ctx->undo->rhm010LinkHost[0])));
    rowreq_ctx->undo->rhm010LinkHost_len =
        rowreq_ctx->data.rhm010LinkHost_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkHost_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010LinkHost_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkHost_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkHost_val_ptr
 */
int
rhm010LinkHost_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                   char *rhm010LinkHost_val_ptr,
                   size_t rhm010LinkHost_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkHost_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkHost_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010LinkHost value.
     * set rhm010LinkHost value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010LinkHost, rhm010LinkHost_val_ptr,
           rhm010LinkHost_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010LinkHost_len =
        rhm010LinkHost_val_ptr_len / sizeof(rhm010LinkHost_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010LinkHost_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010LinkHost_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkHost_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010LinkHost undo.
     */
    /*
     * copy rhm010LinkHost and rhm010LinkHost_len data
     * set rowreq_ctx->data.rhm010LinkHost from rowreq_ctx->undo->rhm010LinkHost
     */
    memcpy(rowreq_ctx->data.rhm010LinkHost,
           rowreq_ctx->undo->rhm010LinkHost,
           (rowreq_ctx->undo->rhm010LinkHost_len *
            sizeof(rowreq_ctx->data.rhm010LinkHost[0])));
    rowreq_ctx->data.rhm010LinkHost_len =
        rowreq_ctx->undo->rhm010LinkHost_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkHost_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkPort
 * rhm010LinkPort is subid 3 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.3
 * Description:
Link port
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   1
 *   hint: d
 *
 *
 * Its syntax is Uint16 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkPort_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010LinkTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010LinkPort_check_value(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                           long rhm010LinkPort_val)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkPort_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010LinkPort value.
     */

    return MFD_SUCCESS;         /* rhm010LinkPort value not illegal */
}                               /* rhm010LinkPort_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010LinkTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010LinkPort_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkPort_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010LinkPort undo.
     */
    /*
     * copy rhm010LinkPort data
     * set rowreq_ctx->undo->rhm010LinkPort from rowreq_ctx->data.rhm010LinkPort
     */
    rowreq_ctx->undo->rhm010LinkPort = rowreq_ctx->data.rhm010LinkPort;


    return MFD_SUCCESS;
}                               /* rhm010LinkPort_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010LinkPort_val
 *        A long containing the new value.
 */
int
rhm010LinkPort_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                   long rhm010LinkPort_val)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkPort_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010LinkPort value.
     * set rhm010LinkPort value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010LinkPort = rhm010LinkPort_val;

    return MFD_SUCCESS;
}                               /* rhm010LinkPort_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010LinkPort_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkPort_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010LinkPort undo.
     */
    /*
     * copy rhm010LinkPort data
     * set rowreq_ctx->data.rhm010LinkPort from rowreq_ctx->undo->rhm010LinkPort
     */
    rowreq_ctx->data.rhm010LinkPort = rowreq_ctx->undo->rhm010LinkPort;


    return MFD_SUCCESS;
}                               /* rhm010LinkPort_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkTransport
 * rhm010LinkTransport is subid 4 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.4
 * Description:
Link transport
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkTransport_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkTransport_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkTransport_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010LinkTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010LinkTransport).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010LinkTransport_check_value(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                                char *rhm010LinkTransport_val_ptr,
                                size_t rhm010LinkTransport_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTransport_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkTransport_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010LinkTransport value.
     */

    return MFD_SUCCESS;         /* rhm010LinkTransport value not illegal */
}                               /* rhm010LinkTransport_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010LinkTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010LinkTransport_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTransport_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010LinkTransport undo.
     */
    /*
     * copy rhm010LinkTransport and rhm010LinkTransport_len data
     * set rowreq_ctx->undo->rhm010LinkTransport from rowreq_ctx->data.rhm010LinkTransport
     */
    memcpy(rowreq_ctx->undo->rhm010LinkTransport,
           rowreq_ctx->data.rhm010LinkTransport,
           (rowreq_ctx->data.rhm010LinkTransport_len *
            sizeof(rowreq_ctx->undo->rhm010LinkTransport[0])));
    rowreq_ctx->undo->rhm010LinkTransport_len =
        rowreq_ctx->data.rhm010LinkTransport_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkTransport_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010LinkTransport_val_ptr
 *        A char containing the new value.
 * @param rhm010LinkTransport_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010LinkTransport_val_ptr
 */
int
rhm010LinkTransport_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                        char *rhm010LinkTransport_val_ptr,
                        size_t rhm010LinkTransport_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTransport_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010LinkTransport_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010LinkTransport value.
     * set rhm010LinkTransport value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010LinkTransport,
           rhm010LinkTransport_val_ptr, rhm010LinkTransport_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010LinkTransport_len =
        rhm010LinkTransport_val_ptr_len /
        sizeof(rhm010LinkTransport_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010LinkTransport_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010LinkTransport_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTransport_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010LinkTransport undo.
     */
    /*
     * copy rhm010LinkTransport and rhm010LinkTransport_len data
     * set rowreq_ctx->data.rhm010LinkTransport from rowreq_ctx->undo->rhm010LinkTransport
     */
    memcpy(rowreq_ctx->data.rhm010LinkTransport,
           rowreq_ctx->undo->rhm010LinkTransport,
           (rowreq_ctx->undo->rhm010LinkTransport_len *
            sizeof(rowreq_ctx->data.rhm010LinkTransport[0])));
    rowreq_ctx->data.rhm010LinkTransport_len =
        rowreq_ctx->undo->rhm010LinkTransport_len;


    return MFD_SUCCESS;
}                               /* rhm010LinkTransport_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkDurable
 * rhm010LinkDurable is subid 5 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.5
 * Description:
Link durable
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkDurable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010LinkTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010LinkDurable_check_value(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                              u_long rhm010LinkDurable_val)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkDurable_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid rhm010LinkDurable value.
     */

    return MFD_SUCCESS;         /* rhm010LinkDurable value not illegal */
}                               /* rhm010LinkDurable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010LinkTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010LinkTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010LinkDurable_undo_setup(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkDurable_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010LinkDurable undo.
     */
    /*
     * copy rhm010LinkDurable data
     * set rowreq_ctx->undo->rhm010LinkDurable from rowreq_ctx->data.rhm010LinkDurable
     */
    rowreq_ctx->undo->rhm010LinkDurable =
        rowreq_ctx->data.rhm010LinkDurable;


    return MFD_SUCCESS;
}                               /* rhm010LinkDurable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010LinkDurable_val
 *        A long containing the new value.
 */
int
rhm010LinkDurable_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                      u_long rhm010LinkDurable_val)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkDurable_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set rhm010LinkDurable value.
     * set rhm010LinkDurable value in rowreq_ctx->data
     */
    rowreq_ctx->data.rhm010LinkDurable = rhm010LinkDurable_val;

    return MFD_SUCCESS;
}                               /* rhm010LinkDurable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010LinkDurable_undo(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkDurable_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010LinkDurable undo.
     */
    /*
     * copy rhm010LinkDurable data
     * set rowreq_ctx->data.rhm010LinkDurable from rowreq_ctx->undo->rhm010LinkDurable
     */
    rowreq_ctx->data.rhm010LinkDurable =
        rowreq_ctx->undo->rhm010LinkDurable;


    return MFD_SUCCESS;
}                               /* rhm010LinkDurable_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010LinkTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010LinkTable if you don't have dependencies)
 */
int
rhm010LinkTable_check_dependencies(rhm010LinkTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010LinkTable:rhm010LinkTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010LinkTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010LinkTable_check_dependencies */

/** @} */
