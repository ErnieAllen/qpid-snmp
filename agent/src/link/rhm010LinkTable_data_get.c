/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010LinkTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement rhm010LinkTable get routines.
 * TODO:240:M: Implement rhm010LinkTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010LinkTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010LinkTable is subid 1 of rhm010Links.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1, length: 12
 */

/*
 * ---------------------------------------------------------------------
 * * TODO:200:r: Implement rhm010LinkTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param rhm010LinkInternalIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
rhm010LinkTable_indexes_set_tbl_idx(rhm010LinkTable_mib_index * tbl_idx,
                                    u_long rhm010LinkInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_indexes_set_tbl_idx", "called\n"));

    /*
     * rhm010LinkInternalIndex(9)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/r/d/h 
     */
    tbl_idx->rhm010LinkInternalIndex = rhm010LinkInternalIndex_val;


    return MFD_SUCCESS;
}                               /* rhm010LinkTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
rhm010LinkTable_indexes_set(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                            u_long rhm010LinkInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTable_indexes_set",
                "called\n"));

    if (MFD_SUCCESS !=
        rhm010LinkTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx,
                                            rhm010LinkInternalIndex_val))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if (0 != rhm010LinkTable_index_to_oid(&rowreq_ctx->oid_idx,
                                          &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
}                               /* rhm010LinkTable_indexes_set */


/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkVhostRef
 * rhm010LinkVhostRef is subid 1 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.1
 * Description:
Link vhostRef
                     Additional info ( nodeType:property, references:Vhost, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010LinkVhostRef data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkVhostRef_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010LinkVhostRef_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010LinkVhostRef.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010LinkVhostRef_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010LinkVhostRef_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010LinkVhostRef_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                       char **rhm010LinkVhostRef_val_ptr_ptr,
                       size_t *rhm010LinkVhostRef_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010LinkVhostRef_val_ptr_ptr)
                   && (NULL != *rhm010LinkVhostRef_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010LinkVhostRef_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkVhostRef_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkVhostRef data.
     * copy (* rhm010LinkVhostRef_val_ptr_ptr ) data and (* rhm010LinkVhostRef_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010LinkVhostRef data
     */
    if ((NULL == (*rhm010LinkVhostRef_val_ptr_ptr)) ||
        ((*rhm010LinkVhostRef_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010LinkVhostRef_len *
          sizeof(rowreq_ctx->data.rhm010LinkVhostRef[0])))) {
        /*
         * allocate space for rhm010LinkVhostRef data
         */
        (*rhm010LinkVhostRef_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010LinkVhostRef_len *
                   sizeof(rowreq_ctx->data.rhm010LinkVhostRef[0]));
        if (NULL == (*rhm010LinkVhostRef_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010LinkVhostRef)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010LinkVhostRef_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010LinkVhostRef_len *
        sizeof(rowreq_ctx->data.rhm010LinkVhostRef[0]);
    memcpy((*rhm010LinkVhostRef_val_ptr_ptr),
           rowreq_ctx->data.rhm010LinkVhostRef,
           rowreq_ctx->data.rhm010LinkVhostRef_len *
           sizeof(rowreq_ctx->data.rhm010LinkVhostRef[0]));

    return MFD_SUCCESS;
}                               /* rhm010LinkVhostRef_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkHost
 * rhm010LinkHost is subid 2 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.2
 * Description:
Link host
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010LinkHost data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkHost_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010LinkHost_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010LinkHost.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010LinkHost_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010LinkHost_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010LinkHost_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                   char **rhm010LinkHost_val_ptr_ptr,
                   size_t *rhm010LinkHost_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010LinkHost_val_ptr_ptr)
                   && (NULL != *rhm010LinkHost_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010LinkHost_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkHost_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkHost data.
     * copy (* rhm010LinkHost_val_ptr_ptr ) data and (* rhm010LinkHost_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010LinkHost data
     */
    if ((NULL == (*rhm010LinkHost_val_ptr_ptr)) ||
        ((*rhm010LinkHost_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010LinkHost_len *
          sizeof(rowreq_ctx->data.rhm010LinkHost[0])))) {
        /*
         * allocate space for rhm010LinkHost data
         */
        (*rhm010LinkHost_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010LinkHost_len *
                   sizeof(rowreq_ctx->data.rhm010LinkHost[0]));
        if (NULL == (*rhm010LinkHost_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010LinkHost)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010LinkHost_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010LinkHost_len *
        sizeof(rowreq_ctx->data.rhm010LinkHost[0]);
    memcpy((*rhm010LinkHost_val_ptr_ptr), rowreq_ctx->data.rhm010LinkHost,
           rowreq_ctx->data.rhm010LinkHost_len *
           sizeof(rowreq_ctx->data.rhm010LinkHost[0]));

    return MFD_SUCCESS;
}                               /* rhm010LinkHost_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkPort
 * rhm010LinkPort is subid 3 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.3
 * Description:
Link port
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   1
 *   settable   1
 *   hint: d
 *
 *
 * Its syntax is Uint16 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the rhm010LinkPort data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkPort_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010LinkPort_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                   long *rhm010LinkPort_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010LinkPort_val_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkPort_get", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkPort data.
     * copy (* rhm010LinkPort_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010LinkPort_val_ptr) = rowreq_ctx->data.rhm010LinkPort;

    return MFD_SUCCESS;
}                               /* rhm010LinkPort_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkTransport
 * rhm010LinkTransport is subid 4 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.4
 * Description:
Link transport
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010LinkTransport data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkTransport_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010LinkTransport_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010LinkTransport.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010LinkTransport_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010LinkTransport_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010LinkTransport_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                        char **rhm010LinkTransport_val_ptr_ptr,
                        size_t *rhm010LinkTransport_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010LinkTransport_val_ptr_ptr)
                   && (NULL != *rhm010LinkTransport_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010LinkTransport_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkTransport_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkTransport data.
     * copy (* rhm010LinkTransport_val_ptr_ptr ) data and (* rhm010LinkTransport_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010LinkTransport data
     */
    if ((NULL == (*rhm010LinkTransport_val_ptr_ptr)) ||
        ((*rhm010LinkTransport_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010LinkTransport_len *
          sizeof(rowreq_ctx->data.rhm010LinkTransport[0])))) {
        /*
         * allocate space for rhm010LinkTransport data
         */
        (*rhm010LinkTransport_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010LinkTransport_len *
                   sizeof(rowreq_ctx->data.rhm010LinkTransport[0]));
        if (NULL == (*rhm010LinkTransport_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010LinkTransport)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010LinkTransport_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010LinkTransport_len *
        sizeof(rowreq_ctx->data.rhm010LinkTransport[0]);
    memcpy((*rhm010LinkTransport_val_ptr_ptr),
           rowreq_ctx->data.rhm010LinkTransport,
           rowreq_ctx->data.rhm010LinkTransport_len *
           sizeof(rowreq_ctx->data.rhm010LinkTransport[0]));

    return MFD_SUCCESS;
}                               /* rhm010LinkTransport_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkDurable
 * rhm010LinkDurable is subid 5 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.5
 * Description:
Link durable
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the rhm010LinkDurable data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkDurable_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010LinkDurable_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                      u_long * rhm010LinkDurable_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010LinkDurable_val_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkDurable_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkDurable data.
     * copy (* rhm010LinkDurable_val_ptr ) from rowreq_ctx->data
     */
    (*rhm010LinkDurable_val_ptr) = rowreq_ctx->data.rhm010LinkDurable;

    return MFD_SUCCESS;
}                               /* rhm010LinkDurable_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkState
 * rhm010LinkState is subid 6 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.6
 * Description:
Operational state of the link
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010LinkState data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkState_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010LinkState_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010LinkState.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010LinkState_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010LinkState_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010LinkState_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                    char **rhm010LinkState_val_ptr_ptr,
                    size_t *rhm010LinkState_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010LinkState_val_ptr_ptr)
                   && (NULL != *rhm010LinkState_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010LinkState_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkState_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkState data.
     * copy (* rhm010LinkState_val_ptr_ptr ) data and (* rhm010LinkState_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010LinkState data
     */
    if ((NULL == (*rhm010LinkState_val_ptr_ptr)) ||
        ((*rhm010LinkState_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010LinkState_len *
          sizeof(rowreq_ctx->data.rhm010LinkState[0])))) {
        /*
         * allocate space for rhm010LinkState data
         */
        (*rhm010LinkState_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010LinkState_len *
                   sizeof(rowreq_ctx->data.rhm010LinkState[0]));
        if (NULL == (*rhm010LinkState_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010LinkState)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010LinkState_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010LinkState_len *
        sizeof(rowreq_ctx->data.rhm010LinkState[0]);
    memcpy((*rhm010LinkState_val_ptr_ptr),
           rowreq_ctx->data.rhm010LinkState,
           rowreq_ctx->data.rhm010LinkState_len *
           sizeof(rowreq_ctx->data.rhm010LinkState[0]));

    return MFD_SUCCESS;
}                               /* rhm010LinkState_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010LinkEntry.rhm010LinkLastError
 * rhm010LinkLastError is subid 7 of rhm010LinkEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.11.1.1.7
 * Description:
Reason link is not operational
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Lstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Extract the current value of the rhm010LinkLastError data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010LinkLastError_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010LinkLastError_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010LinkLastError.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010LinkLastError_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010LinkLastError_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010LinkLastError_get(rhm010LinkTable_rowreq_ctx * rowreq_ctx,
                        char **rhm010LinkLastError_val_ptr_ptr,
                        size_t *rhm010LinkLastError_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010LinkLastError_val_ptr_ptr)
                   && (NULL != *rhm010LinkLastError_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010LinkLastError_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010LinkTable:rhm010LinkLastError_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010LinkLastError data.
     * copy (* rhm010LinkLastError_val_ptr_ptr ) data and (* rhm010LinkLastError_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010LinkLastError data
     */
    if ((NULL == (*rhm010LinkLastError_val_ptr_ptr)) ||
        ((*rhm010LinkLastError_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010LinkLastError_len *
          sizeof(rowreq_ctx->data.rhm010LinkLastError[0])))) {
        /*
         * allocate space for rhm010LinkLastError data
         */
        (*rhm010LinkLastError_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010LinkLastError_len *
                   sizeof(rowreq_ctx->data.rhm010LinkLastError[0]));
        if (NULL == (*rhm010LinkLastError_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010LinkLastError)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010LinkLastError_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010LinkLastError_len *
        sizeof(rowreq_ctx->data.rhm010LinkLastError[0]);
    memcpy((*rhm010LinkLastError_val_ptr_ptr),
           rowreq_ctx->data.rhm010LinkLastError,
           rowreq_ctx->data.rhm010LinkLastError_len *
           sizeof(rowreq_ctx->data.rhm010LinkLastError[0]));

    return MFD_SUCCESS;
}                               /* rhm010LinkLastError_get */



/** @} */
