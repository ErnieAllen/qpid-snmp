/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010BindingTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010BindingTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010BindingTable is subid 1 of rhm010Bindings.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1, length: 12
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * rhm010BindingTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * rhm010BindingTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010BindingTable_undo_setup(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_undo_setup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup rhm010BindingTable undo.
     * set up rhm010BindingTable undo information, in preparation for a set.
     * Undo storage is in (* rhm010BindingMsgMatched_val_ptr )*
     */

    return rc;
}                               /* rhm010BindingTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rhm010BindingTable_undo(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_undo",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> rhm010BindingTable undo.
     * rhm010BindingTable undo information, in response to a failed set.
     * Undo storage is in (* rhm010BindingMsgMatched_val_ptr )*
     */

    return rc;
}                               /* rhm010BindingTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BindingTable_undo_cleanup(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup rhm010BindingTable undo.
     * Undo storage is in (* rhm010BindingMsgMatched_val_ptr )*
     */

    return rc;
}                               /* rhm010BindingTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BindingTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010BindingTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BindingTable_commit(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rhm010BindingTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RHM010BINDINGEXCHANGEREF_FLAG) {
        save_flags &= ~COLUMN_RHM010BINDINGEXCHANGEREF_FLAG;    /* clear rhm010BindingExchangeRef */
        /*
         * TODO:482:o: |-> commit column rhm010BindingExchangeRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BindingTable column rhm010BindingExchangeRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BindingExchangeRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BINDINGEXCHANGEREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BINDINGQUEUEREF_FLAG) {
        save_flags &= ~COLUMN_RHM010BINDINGQUEUEREF_FLAG;       /* clear rhm010BindingQueueRef */
        /*
         * TODO:482:o: |-> commit column rhm010BindingQueueRef.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BindingTable column rhm010BindingQueueRef commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BindingQueueRef
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BINDINGQUEUEREF_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BINDINGBINDINGKEY_FLAG) {
        save_flags &= ~COLUMN_RHM010BINDINGBINDINGKEY_FLAG;     /* clear rhm010BindingBindingKey */
        /*
         * TODO:482:o: |-> commit column rhm010BindingBindingKey.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BindingTable column rhm010BindingBindingKey commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BindingBindingKey
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BINDINGBINDINGKEY_FLAG;
        }
    }

    if (save_flags & COLUMN_RHM010BINDINGARGUMENTS_FLAG) {
        save_flags &= ~COLUMN_RHM010BINDINGARGUMENTS_FLAG;      /* clear rhm010BindingArguments */
        /*
         * TODO:482:o: |-> commit column rhm010BindingArguments.
         */
        rc = -1;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "rhm010BindingTable column rhm010BindingArguments commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo rhm010BindingArguments
             */
            rowreq_ctx->column_set_flags |=
                COLUMN_RHM010BINDINGARGUMENTS_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* rhm010BindingTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BindingTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rhm010BindingTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rhm010BindingTable_undo_commit(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo rhm010BindingTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* rhm010BindingTable_undo_commit */

/*
 * TODO:440:M: Implement rhm010BindingTable node value checks.
 * TODO:450:M: Implement rhm010BindingTable undo functions.
 * TODO:460:M: Implement rhm010BindingTable set functions.
 * TODO:480:M: Implement rhm010BindingTable commit functions.
 */
/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingExchangeRef
 * rhm010BindingExchangeRef is subid 1 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.1
 * Description:
Binding exchangeRef
                     Additional info ( nodeType:property, references:Exchange, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingExchangeRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingExchangeRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingExchangeRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BindingTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BindingExchangeRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BindingExchangeRef_check_value(rhm010BindingTable_rowreq_ctx *
                                     rowreq_ctx,
                                     char
                                     *rhm010BindingExchangeRef_val_ptr,
                                     size_t
                                     rhm010BindingExchangeRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingExchangeRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingExchangeRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BindingExchangeRef value.
     */

    return MFD_SUCCESS;         /* rhm010BindingExchangeRef value not illegal */
}                               /* rhm010BindingExchangeRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BindingTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BindingExchangeRef_undo_setup(rhm010BindingTable_rowreq_ctx *
                                    rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingExchangeRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BindingExchangeRef undo.
     */
    /*
     * copy rhm010BindingExchangeRef and rhm010BindingExchangeRef_len data
     * set rowreq_ctx->undo->rhm010BindingExchangeRef from rowreq_ctx->data.rhm010BindingExchangeRef
     */
    memcpy(rowreq_ctx->undo->rhm010BindingExchangeRef,
           rowreq_ctx->data.rhm010BindingExchangeRef,
           (rowreq_ctx->data.rhm010BindingExchangeRef_len *
            sizeof(rowreq_ctx->undo->rhm010BindingExchangeRef[0])));
    rowreq_ctx->undo->rhm010BindingExchangeRef_len =
        rowreq_ctx->data.rhm010BindingExchangeRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingExchangeRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BindingExchangeRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingExchangeRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingExchangeRef_val_ptr
 */
int
rhm010BindingExchangeRef_set(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                             char *rhm010BindingExchangeRef_val_ptr,
                             size_t rhm010BindingExchangeRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingExchangeRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingExchangeRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BindingExchangeRef value.
     * set rhm010BindingExchangeRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BindingExchangeRef,
           rhm010BindingExchangeRef_val_ptr,
           rhm010BindingExchangeRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BindingExchangeRef_len =
        rhm010BindingExchangeRef_val_ptr_len /
        sizeof(rhm010BindingExchangeRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BindingExchangeRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BindingExchangeRef_undo(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingExchangeRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BindingExchangeRef undo.
     */
    /*
     * copy rhm010BindingExchangeRef and rhm010BindingExchangeRef_len data
     * set rowreq_ctx->data.rhm010BindingExchangeRef from rowreq_ctx->undo->rhm010BindingExchangeRef
     */
    memcpy(rowreq_ctx->data.rhm010BindingExchangeRef,
           rowreq_ctx->undo->rhm010BindingExchangeRef,
           (rowreq_ctx->undo->rhm010BindingExchangeRef_len *
            sizeof(rowreq_ctx->data.rhm010BindingExchangeRef[0])));
    rowreq_ctx->data.rhm010BindingExchangeRef_len =
        rowreq_ctx->undo->rhm010BindingExchangeRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingExchangeRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingQueueRef
 * rhm010BindingQueueRef is subid 2 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.2
 * Description:
Binding queueRef
                     Additional info ( nodeType:property, references:Queue, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingQueueRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingQueueRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingQueueRef_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BindingTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BindingQueueRef).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BindingQueueRef_check_value(rhm010BindingTable_rowreq_ctx *
                                  rowreq_ctx,
                                  char *rhm010BindingQueueRef_val_ptr,
                                  size_t rhm010BindingQueueRef_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingQueueRef_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingQueueRef_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BindingQueueRef value.
     */

    return MFD_SUCCESS;         /* rhm010BindingQueueRef value not illegal */
}                               /* rhm010BindingQueueRef_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BindingTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BindingQueueRef_undo_setup(rhm010BindingTable_rowreq_ctx *
                                 rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingQueueRef_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BindingQueueRef undo.
     */
    /*
     * copy rhm010BindingQueueRef and rhm010BindingQueueRef_len data
     * set rowreq_ctx->undo->rhm010BindingQueueRef from rowreq_ctx->data.rhm010BindingQueueRef
     */
    memcpy(rowreq_ctx->undo->rhm010BindingQueueRef,
           rowreq_ctx->data.rhm010BindingQueueRef,
           (rowreq_ctx->data.rhm010BindingQueueRef_len *
            sizeof(rowreq_ctx->undo->rhm010BindingQueueRef[0])));
    rowreq_ctx->undo->rhm010BindingQueueRef_len =
        rowreq_ctx->data.rhm010BindingQueueRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingQueueRef_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BindingQueueRef_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingQueueRef_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingQueueRef_val_ptr
 */
int
rhm010BindingQueueRef_set(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                          char *rhm010BindingQueueRef_val_ptr,
                          size_t rhm010BindingQueueRef_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingQueueRef_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingQueueRef_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BindingQueueRef value.
     * set rhm010BindingQueueRef value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BindingQueueRef,
           rhm010BindingQueueRef_val_ptr,
           rhm010BindingQueueRef_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BindingQueueRef_len =
        rhm010BindingQueueRef_val_ptr_len /
        sizeof(rhm010BindingQueueRef_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BindingQueueRef_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BindingQueueRef_undo(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingQueueRef_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BindingQueueRef undo.
     */
    /*
     * copy rhm010BindingQueueRef and rhm010BindingQueueRef_len data
     * set rowreq_ctx->data.rhm010BindingQueueRef from rowreq_ctx->undo->rhm010BindingQueueRef
     */
    memcpy(rowreq_ctx->data.rhm010BindingQueueRef,
           rowreq_ctx->undo->rhm010BindingQueueRef,
           (rowreq_ctx->undo->rhm010BindingQueueRef_len *
            sizeof(rowreq_ctx->data.rhm010BindingQueueRef[0])));
    rowreq_ctx->data.rhm010BindingQueueRef_len =
        rowreq_ctx->undo->rhm010BindingQueueRef_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingQueueRef_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingBindingKey
 * rhm010BindingBindingKey is subid 3 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.3
 * Description:
Binding bindingKey
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Lstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingBindingKey_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingBindingKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingBindingKey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BindingTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BindingBindingKey).
 *    The length is in (one of) the range set(s):  0 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BindingBindingKey_check_value(rhm010BindingTable_rowreq_ctx *
                                    rowreq_ctx,
                                    char *rhm010BindingBindingKey_val_ptr,
                                    size_t
                                    rhm010BindingBindingKey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingBindingKey_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingBindingKey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BindingBindingKey value.
     */

    return MFD_SUCCESS;         /* rhm010BindingBindingKey value not illegal */
}                               /* rhm010BindingBindingKey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BindingTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BindingBindingKey_undo_setup(rhm010BindingTable_rowreq_ctx *
                                   rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingBindingKey_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BindingBindingKey undo.
     */
    /*
     * copy rhm010BindingBindingKey and rhm010BindingBindingKey_len data
     * set rowreq_ctx->undo->rhm010BindingBindingKey from rowreq_ctx->data.rhm010BindingBindingKey
     */
    memcpy(rowreq_ctx->undo->rhm010BindingBindingKey,
           rowreq_ctx->data.rhm010BindingBindingKey,
           (rowreq_ctx->data.rhm010BindingBindingKey_len *
            sizeof(rowreq_ctx->undo->rhm010BindingBindingKey[0])));
    rowreq_ctx->undo->rhm010BindingBindingKey_len =
        rowreq_ctx->data.rhm010BindingBindingKey_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingBindingKey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BindingBindingKey_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingBindingKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingBindingKey_val_ptr
 */
int
rhm010BindingBindingKey_set(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                            char *rhm010BindingBindingKey_val_ptr,
                            size_t rhm010BindingBindingKey_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingBindingKey_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingBindingKey_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BindingBindingKey value.
     * set rhm010BindingBindingKey value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BindingBindingKey,
           rhm010BindingBindingKey_val_ptr,
           rhm010BindingBindingKey_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BindingBindingKey_len =
        rhm010BindingBindingKey_val_ptr_len /
        sizeof(rhm010BindingBindingKey_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BindingBindingKey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BindingBindingKey_undo(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingBindingKey_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BindingBindingKey undo.
     */
    /*
     * copy rhm010BindingBindingKey and rhm010BindingBindingKey_len data
     * set rowreq_ctx->data.rhm010BindingBindingKey from rowreq_ctx->undo->rhm010BindingBindingKey
     */
    memcpy(rowreq_ctx->data.rhm010BindingBindingKey,
           rowreq_ctx->undo->rhm010BindingBindingKey,
           (rowreq_ctx->undo->rhm010BindingBindingKey_len *
            sizeof(rowreq_ctx->data.rhm010BindingBindingKey[0])));
    rowreq_ctx->data.rhm010BindingBindingKey_len =
        rowreq_ctx->undo->rhm010BindingBindingKey_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingBindingKey_undo */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingArguments
 * rhm010BindingArguments is subid 4 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.4
 * Description:
Binding arguments
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Map (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingArguments_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingArguments_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingArguments_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rhm010BindingTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rhm010BindingArguments).
 *    The length is in (one of) the range set(s):  0 - 65535
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rhm010BindingArguments_check_value(rhm010BindingTable_rowreq_ctx *
                                   rowreq_ctx,
                                   char *rhm010BindingArguments_val_ptr,
                                   size_t
                                   rhm010BindingArguments_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingArguments_check_value", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingArguments_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rhm010BindingArguments value.
     */

    return MFD_SUCCESS;         /* rhm010BindingArguments value not illegal */
}                               /* rhm010BindingArguments_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rhm010BindingTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rhm010BindingTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rhm010BindingArguments_undo_setup(rhm010BindingTable_rowreq_ctx *
                                  rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingArguments_undo_setup", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rhm010BindingArguments undo.
     */
    /*
     * copy rhm010BindingArguments and rhm010BindingArguments_len data
     * set rowreq_ctx->undo->rhm010BindingArguments from rowreq_ctx->data.rhm010BindingArguments
     */
    memcpy(rowreq_ctx->undo->rhm010BindingArguments,
           rowreq_ctx->data.rhm010BindingArguments,
           (rowreq_ctx->data.rhm010BindingArguments_len *
            sizeof(rowreq_ctx->undo->rhm010BindingArguments[0])));
    rowreq_ctx->undo->rhm010BindingArguments_len =
        rowreq_ctx->data.rhm010BindingArguments_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingArguments_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rhm010BindingArguments_val_ptr
 *        A char containing the new value.
 * @param rhm010BindingArguments_val_ptr_len
 *        The size (in bytes) of the data pointed to by rhm010BindingArguments_val_ptr
 */
int
rhm010BindingArguments_set(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                           char *rhm010BindingArguments_val_ptr,
                           size_t rhm010BindingArguments_val_ptr_len)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingArguments_set",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rhm010BindingArguments_val_ptr);

    /*
     * TODO:461:M: |-> Set rhm010BindingArguments value.
     * set rhm010BindingArguments value in rowreq_ctx->data
     */
    memcpy(rowreq_ctx->data.rhm010BindingArguments,
           rhm010BindingArguments_val_ptr,
           rhm010BindingArguments_val_ptr_len);
    /** convert bytes to number of char */
    rowreq_ctx->data.rhm010BindingArguments_len =
        rhm010BindingArguments_val_ptr_len /
        sizeof(rhm010BindingArguments_val_ptr[0]);

    return MFD_SUCCESS;
}                               /* rhm010BindingArguments_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rhm010BindingArguments_undo(rhm010BindingTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingArguments_undo",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rhm010BindingArguments undo.
     */
    /*
     * copy rhm010BindingArguments and rhm010BindingArguments_len data
     * set rowreq_ctx->data.rhm010BindingArguments from rowreq_ctx->undo->rhm010BindingArguments
     */
    memcpy(rowreq_ctx->data.rhm010BindingArguments,
           rowreq_ctx->undo->rhm010BindingArguments,
           (rowreq_ctx->undo->rhm010BindingArguments_len *
            sizeof(rowreq_ctx->data.rhm010BindingArguments[0])));
    rowreq_ctx->data.rhm010BindingArguments_len =
        rowreq_ctx->undo->rhm010BindingArguments_len;


    return MFD_SUCCESS;
}                               /* rhm010BindingArguments_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rhm010BindingTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-rhm010BindingTable if you don't have dependencies)
 */
int
rhm010BindingTable_check_dependencies(rhm010BindingTable_rowreq_ctx *
                                      rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:rhm010BindingTable:rhm010BindingTable_check_dependencies", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check rhm010BindingTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* rhm010BindingTable_check_dependencies */

/** @} */
