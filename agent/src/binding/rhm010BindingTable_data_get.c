/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "rhm010BindingTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement rhm010BindingTable get routines.
 * TODO:240:M: Implement rhm010BindingTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rhm010BindingTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MRG-MESSAGING-MIB::rhm010BindingTable is subid 1 of rhm010Bindings.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1, length: 12
 */

/*
 * ---------------------------------------------------------------------
 * * TODO:200:r: Implement rhm010BindingTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param rhm010BindingInternalIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
rhm010BindingTable_indexes_set_tbl_idx(rhm010BindingTable_mib_index *
                                       tbl_idx,
                                       u_long
                                       rhm010BindingInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_indexes_set_tbl_idx", "called\n"));

    /*
     * rhm010BindingInternalIndex(7)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/r/d/h 
     */
    tbl_idx->rhm010BindingInternalIndex = rhm010BindingInternalIndex_val;


    return MFD_SUCCESS;
}                               /* rhm010BindingTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
rhm010BindingTable_indexes_set(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                               u_long rhm010BindingInternalIndex_val)
{
    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingTable_indexes_set", "called\n"));

    if (MFD_SUCCESS !=
        rhm010BindingTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx,
                                               rhm010BindingInternalIndex_val))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if (0 != rhm010BindingTable_index_to_oid(&rowreq_ctx->oid_idx,
                                             &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
}                               /* rhm010BindingTable_indexes_set */


/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingExchangeRef
 * rhm010BindingExchangeRef is subid 1 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.1
 * Description:
Binding exchangeRef
                     Additional info ( nodeType:property, references:Exchange, index:y, parentRef:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010BindingExchangeRef data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingExchangeRef_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010BindingExchangeRef_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010BindingExchangeRef.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010BindingExchangeRef_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010BindingExchangeRef_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010BindingExchangeRef_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                             char **rhm010BindingExchangeRef_val_ptr_ptr,
                             size_t
                             *rhm010BindingExchangeRef_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010BindingExchangeRef_val_ptr_ptr)
                   && (NULL != *rhm010BindingExchangeRef_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010BindingExchangeRef_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingExchangeRef_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010BindingExchangeRef data.
     * copy (* rhm010BindingExchangeRef_val_ptr_ptr ) data and (* rhm010BindingExchangeRef_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010BindingExchangeRef data
     */
    if ((NULL == (*rhm010BindingExchangeRef_val_ptr_ptr)) ||
        ((*rhm010BindingExchangeRef_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010BindingExchangeRef_len *
          sizeof(rowreq_ctx->data.rhm010BindingExchangeRef[0])))) {
        /*
         * allocate space for rhm010BindingExchangeRef data
         */
        (*rhm010BindingExchangeRef_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010BindingExchangeRef_len *
                   sizeof(rowreq_ctx->data.rhm010BindingExchangeRef[0]));
        if (NULL == (*rhm010BindingExchangeRef_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010BindingExchangeRef)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010BindingExchangeRef_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010BindingExchangeRef_len *
        sizeof(rowreq_ctx->data.rhm010BindingExchangeRef[0]);
    memcpy((*rhm010BindingExchangeRef_val_ptr_ptr),
           rowreq_ctx->data.rhm010BindingExchangeRef,
           rowreq_ctx->data.rhm010BindingExchangeRef_len *
           sizeof(rowreq_ctx->data.rhm010BindingExchangeRef[0]));

    return MFD_SUCCESS;
}                               /* rhm010BindingExchangeRef_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingQueueRef
 * rhm010BindingQueueRef is subid 2 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.2
 * Description:
Binding queueRef
                     Additional info ( nodeType:property, references:Queue, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is ObjId (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010BindingQueueRef data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingQueueRef_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010BindingQueueRef_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010BindingQueueRef.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010BindingQueueRef_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010BindingQueueRef_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010BindingQueueRef_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                          char **rhm010BindingQueueRef_val_ptr_ptr,
                          size_t *rhm010BindingQueueRef_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010BindingQueueRef_val_ptr_ptr)
                   && (NULL != *rhm010BindingQueueRef_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010BindingQueueRef_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingQueueRef_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010BindingQueueRef data.
     * copy (* rhm010BindingQueueRef_val_ptr_ptr ) data and (* rhm010BindingQueueRef_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010BindingQueueRef data
     */
    if ((NULL == (*rhm010BindingQueueRef_val_ptr_ptr)) ||
        ((*rhm010BindingQueueRef_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010BindingQueueRef_len *
          sizeof(rowreq_ctx->data.rhm010BindingQueueRef[0])))) {
        /*
         * allocate space for rhm010BindingQueueRef data
         */
        (*rhm010BindingQueueRef_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010BindingQueueRef_len *
                   sizeof(rowreq_ctx->data.rhm010BindingQueueRef[0]));
        if (NULL == (*rhm010BindingQueueRef_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010BindingQueueRef)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010BindingQueueRef_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010BindingQueueRef_len *
        sizeof(rowreq_ctx->data.rhm010BindingQueueRef[0]);
    memcpy((*rhm010BindingQueueRef_val_ptr_ptr),
           rowreq_ctx->data.rhm010BindingQueueRef,
           rowreq_ctx->data.rhm010BindingQueueRef_len *
           sizeof(rowreq_ctx->data.rhm010BindingQueueRef[0]));

    return MFD_SUCCESS;
}                               /* rhm010BindingQueueRef_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingBindingKey
 * rhm010BindingBindingKey is subid 3 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.3
 * Description:
Binding bindingKey
                     Additional info ( nodeType:property, index:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Lstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Extract the current value of the rhm010BindingBindingKey data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingBindingKey_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010BindingBindingKey_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010BindingBindingKey.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010BindingBindingKey_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010BindingBindingKey_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010BindingBindingKey_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                            char **rhm010BindingBindingKey_val_ptr_ptr,
                            size_t
                            *rhm010BindingBindingKey_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010BindingBindingKey_val_ptr_ptr)
                   && (NULL != *rhm010BindingBindingKey_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010BindingBindingKey_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingBindingKey_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010BindingBindingKey data.
     * copy (* rhm010BindingBindingKey_val_ptr_ptr ) data and (* rhm010BindingBindingKey_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010BindingBindingKey data
     */
    if ((NULL == (*rhm010BindingBindingKey_val_ptr_ptr)) ||
        ((*rhm010BindingBindingKey_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010BindingBindingKey_len *
          sizeof(rowreq_ctx->data.rhm010BindingBindingKey[0])))) {
        /*
         * allocate space for rhm010BindingBindingKey data
         */
        (*rhm010BindingBindingKey_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010BindingBindingKey_len *
                   sizeof(rowreq_ctx->data.rhm010BindingBindingKey[0]));
        if (NULL == (*rhm010BindingBindingKey_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010BindingBindingKey)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010BindingBindingKey_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010BindingBindingKey_len *
        sizeof(rowreq_ctx->data.rhm010BindingBindingKey[0]);
    memcpy((*rhm010BindingBindingKey_val_ptr_ptr),
           rowreq_ctx->data.rhm010BindingBindingKey,
           rowreq_ctx->data.rhm010BindingBindingKey_len *
           sizeof(rowreq_ctx->data.rhm010BindingBindingKey[0]));

    return MFD_SUCCESS;
}                               /* rhm010BindingBindingKey_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingArguments
 * rhm010BindingArguments is subid 4 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.4
 * Description:
Binding arguments
                     Additional info ( nodeType:property )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 65535a
 *
 * Ranges:  0 - 65535;
 *
 * Its syntax is Map (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 65535)
 */
/**
 * Extract the current value of the rhm010BindingArguments data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingArguments_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010BindingArguments_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010BindingArguments.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010BindingArguments_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010BindingArguments_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010BindingArguments_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                           char **rhm010BindingArguments_val_ptr_ptr,
                           size_t *rhm010BindingArguments_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010BindingArguments_val_ptr_ptr)
                   && (NULL != *rhm010BindingArguments_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010BindingArguments_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingArguments_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010BindingArguments data.
     * copy (* rhm010BindingArguments_val_ptr_ptr ) data and (* rhm010BindingArguments_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010BindingArguments data
     */
    if ((NULL == (*rhm010BindingArguments_val_ptr_ptr)) ||
        ((*rhm010BindingArguments_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010BindingArguments_len *
          sizeof(rowreq_ctx->data.rhm010BindingArguments[0])))) {
        /*
         * allocate space for rhm010BindingArguments data
         */
        (*rhm010BindingArguments_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010BindingArguments_len *
                   sizeof(rowreq_ctx->data.rhm010BindingArguments[0]));
        if (NULL == (*rhm010BindingArguments_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010BindingArguments)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010BindingArguments_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010BindingArguments_len *
        sizeof(rowreq_ctx->data.rhm010BindingArguments[0]);
    memcpy((*rhm010BindingArguments_val_ptr_ptr),
           rowreq_ctx->data.rhm010BindingArguments,
           rowreq_ctx->data.rhm010BindingArguments_len *
           sizeof(rowreq_ctx->data.rhm010BindingArguments[0]));

    return MFD_SUCCESS;
}                               /* rhm010BindingArguments_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingOrigin
 * rhm010BindingOrigin is subid 5 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.5
 * Description:
Binding origin
                     Additional info ( nodeType:property, optional:y )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is Sstr (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the rhm010BindingOrigin data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingOrigin_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param rhm010BindingOrigin_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by rhm010BindingOrigin.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*rhm010BindingOrigin_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update rhm010BindingOrigin_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
rhm010BindingOrigin_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                        char **rhm010BindingOrigin_val_ptr_ptr,
                        size_t *rhm010BindingOrigin_val_ptr_len_ptr)
{
   /** we should have a non-NULL pointer and enough storage */
    netsnmp_assert((NULL != rhm010BindingOrigin_val_ptr_ptr)
                   && (NULL != *rhm010BindingOrigin_val_ptr_ptr));
    netsnmp_assert(NULL != rhm010BindingOrigin_val_ptr_len_ptr);


    DEBUGMSGTL(("verbose:rhm010BindingTable:rhm010BindingOrigin_get",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:231:o: |-> Extract the current value of the rhm010BindingOrigin data.
     * copy (* rhm010BindingOrigin_val_ptr_ptr ) data and (* rhm010BindingOrigin_val_ptr_len_ptr ) from rowreq_ctx->data
     */
    /*
     * make sure there is enough space for rhm010BindingOrigin data
     */
    if ((NULL == (*rhm010BindingOrigin_val_ptr_ptr)) ||
        ((*rhm010BindingOrigin_val_ptr_len_ptr) <
         (rowreq_ctx->data.rhm010BindingOrigin_len *
          sizeof(rowreq_ctx->data.rhm010BindingOrigin[0])))) {
        /*
         * allocate space for rhm010BindingOrigin data
         */
        (*rhm010BindingOrigin_val_ptr_ptr) =
            malloc(rowreq_ctx->data.rhm010BindingOrigin_len *
                   sizeof(rowreq_ctx->data.rhm010BindingOrigin[0]));
        if (NULL == (*rhm010BindingOrigin_val_ptr_ptr)) {
            snmp_log(LOG_ERR,
                     "could not allocate memory (rowreq_ctx->data.rhm010BindingOrigin)\n");
            return MFD_ERROR;
        }
    }
    (*rhm010BindingOrigin_val_ptr_len_ptr) =
        rowreq_ctx->data.rhm010BindingOrigin_len *
        sizeof(rowreq_ctx->data.rhm010BindingOrigin[0]);
    memcpy((*rhm010BindingOrigin_val_ptr_ptr),
           rowreq_ctx->data.rhm010BindingOrigin,
           rowreq_ctx->data.rhm010BindingOrigin_len *
           sizeof(rowreq_ctx->data.rhm010BindingOrigin[0]));

    return MFD_SUCCESS;
}                               /* rhm010BindingOrigin_get */

/*---------------------------------------------------------------------
 * MRG-MESSAGING-MIB::rhm010BindingEntry.rhm010BindingMsgMatched
 * rhm010BindingMsgMatched is subid 6 of rhm010BindingEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.2312.5672.1.1.8.1.1.6
 * Description:
Binding msgMatched
                     Additional info ( nodeType:statistic )
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER64 (based on perltype COUNTER64)
 * The net-snmp type is ASN_COUNTER64. The C type decl is U64 (U64)
 */
/**
 * Extract the current value of the rhm010BindingMsgMatched data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rhm010BindingMsgMatched_val_ptr
 *        Pointer to storage for a U64 variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
rhm010BindingMsgMatched_get(rhm010BindingTable_rowreq_ctx * rowreq_ctx,
                            U64 * rhm010BindingMsgMatched_val_ptr)
{
   /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rhm010BindingMsgMatched_val_ptr);

    /*
     * TODO:231:o: |-> copy rhm010BindingMsgMatched data.
     * get (* rhm010BindingMsgMatched_val_ptr ).low and (* rhm010BindingMsgMatched_val_ptr ).high from rowreq_ctx->data
     */
    (*rhm010BindingMsgMatched_val_ptr).high =
        rowreq_ctx->data.rhm010BindingMsgMatched.high;
    (*rhm010BindingMsgMatched_val_ptr).low =
        rowreq_ctx->data.rhm010BindingMsgMatched.low;


    return MFD_SUCCESS;
}                               /* rhm010BindingMsgMatched_get */



/** @} */
