/*
 * Note: this file originally auto-generated by mib2c using
 *        $
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "rhm010Memory.h"

#include "../qpid_api.h"

/** Initializes the rhm010Memory module */
void
init_rhm010Memory(void)
{
    const oid       rhm010MemoryName_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 1 };
    const oid       rhm010MemoryMallocarena_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 2 };
    const oid       rhm010MemoryMallocordblks_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 3 };
    const oid       rhm010MemoryMallochblks_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 4 };
    const oid       rhm010MemoryMallochblkhd_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 5 };
    const oid       rhm010MemoryMallocuordblks_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 6 };
    const oid       rhm010MemoryMallocfordblks_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 7 };
    const oid       rhm010MemoryMallockeepcost_oid[] =
        { 1, 3, 6, 1, 4, 1, 2312, 5672, 1, 1, 2, 8 };

    DEBUGMSGTL(("rhm010Memory", "Initializing\n"));

    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryName", handle_rhm010MemoryName,
                             rhm010MemoryName_oid,
                             OID_LENGTH(rhm010MemoryName_oid),
                             HANDLER_CAN_RWRITE));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallocarena",
                             handle_rhm010MemoryMallocarena,
                             rhm010MemoryMallocarena_oid,
                             OID_LENGTH(rhm010MemoryMallocarena_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallocordblks",
                             handle_rhm010MemoryMallocordblks,
                             rhm010MemoryMallocordblks_oid,
                             OID_LENGTH(rhm010MemoryMallocordblks_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallochblks",
                             handle_rhm010MemoryMallochblks,
                             rhm010MemoryMallochblks_oid,
                             OID_LENGTH(rhm010MemoryMallochblks_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallochblkhd",
                             handle_rhm010MemoryMallochblkhd,
                             rhm010MemoryMallochblkhd_oid,
                             OID_LENGTH(rhm010MemoryMallochblkhd_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallocuordblks",
                             handle_rhm010MemoryMallocuordblks,
                             rhm010MemoryMallocuordblks_oid,
                             OID_LENGTH(rhm010MemoryMallocuordblks_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallocfordblks",
                             handle_rhm010MemoryMallocfordblks,
                             rhm010MemoryMallocfordblks_oid,
                             OID_LENGTH(rhm010MemoryMallocfordblks_oid),
                             HANDLER_CAN_RONLY));
    netsnmp_register_scalar(netsnmp_create_handler_registration
                            ("rhm010MemoryMallockeepcost",
                             handle_rhm010MemoryMallockeepcost,
                             rhm010MemoryMallockeepcost_oid,
                             OID_LENGTH(rhm010MemoryMallockeepcost_oid),
                             HANDLER_CAN_RONLY));
}

int
handle_rhm010MemoryName(netsnmp_mib_handler *handler,
                        netsnmp_handler_registration *reginfo,
                        netsnmp_agent_request_info *reqinfo,
                        netsnmp_request_info *requests)
{
    int             ret;
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

    char data[256];
    qpidGetScalarString("memory", "name", data, 255);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_OCTET_STR,
        		data, 			// a pointer to the scalar's data
        		strlen(data)	// the length of the data in bytes
        );
        break;

        /*
         * SET REQUEST
         *
         * multiple states in the transaction.  See:
         * http://www.net-snmp.org/tutorial-5/toolkit/mib_module/set-actions.jpg
         */
    case MODE_SET_RESERVE1:
        /*
         * or you could use netsnmp_check_vb_type_and_size instead 
         */
        ret = netsnmp_check_vb_type(requests->requestvb, ASN_OCTET_STR);
        if (ret != SNMP_ERR_NOERROR) {
            netsnmp_set_request_error(reqinfo, requests, ret);
        }
        break;

    case MODE_SET_RESERVE2:
        /*
         * XXX malloc "undo" storage buffer 
         */
        if ( 0/* XXX if malloc, or whatever, failed: */ ) {
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_RESOURCEUNAVAILABLE);
        }
        break;

    case MODE_SET_FREE:
        /*
         * XXX: free resources allocated in RESERVE1 and/or
         * RESERVE2.  Something failed somewhere, and the states
         * below won't be called. 
         */
        break;

    case MODE_SET_ACTION:
        /*
         * XXX: perform the value change here 
         */
        if ( 0/* XXX: error? */ ) {
        	netsnmp_set_request_error(reqinfo, requests, SNMP_ERR_GENERR);
        }
        break;

    case MODE_SET_COMMIT:
        /*
         * XXX: delete temporary storage 
         */
        if ( 0/* XXX: error? */ ) {
            /*
             * try _really_really_ hard to never get to this point 
             */
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_COMMITFAILED);
        }
        break;

    case MODE_SET_UNDO:
        /*
         * XXX: UNDO and return to previous value for the object 
         */
        if ( 0/* XXX: error? */ ) {
            /*
             * try _really_really_ hard to never get to this point 
             */
            netsnmp_set_request_error(reqinfo, requests,
                                      SNMP_ERR_UNDOFAILED);
        }
        break;

    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR, "unknown mode (%d) in handle_rhm010MemoryName\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallocarena(netsnmp_mib_handler *handler,
                               netsnmp_handler_registration *reginfo,
                               netsnmp_agent_request_info *reqinfo,
                               netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallocarena", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallocarena\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallocordblks(netsnmp_mib_handler *handler,
                                 netsnmp_handler_registration *reginfo,
                                 netsnmp_agent_request_info *reqinfo,
                                 netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallocordblks", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallocordblks\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallochblks(netsnmp_mib_handler *handler,
                               netsnmp_handler_registration *reginfo,
                               netsnmp_agent_request_info *reqinfo,
                               netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallochblks", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallochblks\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallochblkhd(netsnmp_mib_handler *handler,
                                netsnmp_handler_registration *reginfo,
                                netsnmp_agent_request_info *reqinfo,
                                netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallochblkhd", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallochblkhd\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallocuordblks(netsnmp_mib_handler *handler,
                                  netsnmp_handler_registration *reginfo,
                                  netsnmp_agent_request_info *reqinfo,
                                  netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallocuordblks", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallocuordblks\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallocfordblks(netsnmp_mib_handler *handler,
                                  netsnmp_handler_registration *reginfo,
                                  netsnmp_agent_request_info *reqinfo,
                                  netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "mallocfordblks", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallocfordblks\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}

int
handle_rhm010MemoryMallockeepcost(netsnmp_mib_handler *handler,
                                  netsnmp_handler_registration *reginfo,
                                  netsnmp_agent_request_info *reqinfo,
                                  netsnmp_request_info *requests)
{
    /*
     * We are never called for a GETNEXT if it's registered as a
     * "instance", as it's "magically" handled for us.  
     */

    /*
     * a instance handler also only hands us one request at a time, so
     * we don't need to loop over a list of requests; we'll only get one. 
     */

	uint64_t count64;
	qpidGetScalarU64("memory", "Mallockeepcost", &count64);

    switch (reqinfo->mode) {

    case MODE_GET:
        snmp_set_var_typed_value(requests->requestvb, ASN_COUNTER64,
        		&count64, 			// a pointer to the scalar's data
                sizeof(count64) 	// the length of the data in bytes
        );
        break;


    default:
        /*
         * we should never get here, so this is a really bad error 
         */
        snmp_log(LOG_ERR,
                 "unknown mode (%d) in handle_rhm010MemoryMallockeepcost\n",
                 reqinfo->mode);
        return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
